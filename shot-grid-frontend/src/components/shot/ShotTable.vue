<script setup>import { productionStatusMeta } from '@/utils/productionStatus'; defineProps({ rows: { type: Array, required: true } })</script>
<template><el-table :data="rows" empty-text="暂无镜头"><el-table-column prop="shotCode" label="镜头" width="100" /><el-table-column label="集 / 场"><template #default="{ row }">EP{{ row.episodeNo }} · {{ row.sceneName || row.sceneCode }}</template></el-table-column><el-table-column prop="description" label="制作内容" min-width="240" /><el-table-column prop="producerName" label="制作人"><template #default="{ row }">{{ row.producerName || '未分配' }}</template></el-table-column><el-table-column prop="latestFeedback" label="最新反馈"><template #default="{ row }">{{ row.latestFeedback || '—' }}</template></el-table-column><el-table-column label="聚合状态"><template #default="{ row }"><el-tag :type="productionStatusMeta(row.aggregateStatus).type">{{ productionStatusMeta(row.aggregateStatus).label }}</el-tag></template></el-table-column></el-table></template>
