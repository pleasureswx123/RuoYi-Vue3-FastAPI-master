# Shot Grid 项目完成计划

## 1. 计划目的

本计划用于把《需求.md》中的 ShotGrid 轻量化产品方案交付为可运行、可验证、可继续扩展的 AI 影视项目管理系统。

计划覆盖：

- `shot-grid-frontend` 业务前端；
- `ruoyi-fastapi-backend` Shot Grid 业务接口；
- PostgreSQL 表结构与迁移；
- RuoYi 登录、用户、权限、文件和传输加密能力复用；
- 图片、视频、版本、批注和审核链路；
- 自动化测试、真实 E2E、部署和运维文档。

本计划不以“页面已经画完”作为完成标准。只有真实数据、权限、文件、状态流转和刷新恢复均通过验证，相关功能才算完成。

## 2. 当前状态

截至 2026-08-10 第一版后端领域基础完成时：

- 已有产品需求：`docs/需求.md`；
- 已有开发约束：`../AGENTS.md`；
- 已建立并完成后端基座对齐、集层级、资产制作分项层级、单任务生产闭环、最新镜头/资产文件命名、独立业务端、范围导航和两类 Excel 导入设计的阶段 0 v1.1 契约：`docs/领域模型与API契约.md`；
- 已形成现有代码证据、能力复用边界和分阶段实施路线：`docs/若依基座分析与实施方案.md`；
- `shot-grid-frontend` 尚无可运行源码、`package.json` 或自动化测试；
- 后端已建立 `module_shot_grid` 第一版领域基础：22 张 `sg_` 表 DO、项目访问依赖、受菜单权限约束的 `/shot-grid/navigation`、Alembic 首迁移和 PostgreSQL 初始化基线；项目 CRUD、Excel 导入、NAS I/O、任务动作、版本发布和审核闭环仍未实现；
- 当前主数据库为 PostgreSQL；
- RuoYi 已提供认证、用户、角色、权限、Redis、受保护文件和业务附件能力；
- Shot Grid 的媒体代理、缩略图、视频批注和审核状态机尚未证明已经实现。

因此，项目从“需求和规则已形成”阶段开始，不能直接进入全量页面开发。

## 3. 完成定义

项目完成必须同时满足以下条件。

### 3.1 产品范围

- 六个一级页面可用：工作台、项目、镜头管理、资产库管理、版本审核、文件与 NAS。
- 项目内部页面可用：概况、集与场次、镜头、资产、审核、成员。
- 任务详情、镜头详情、资产详情和版本审核形成连贯业务路径。
- 《需求.md》列出的 14 项 MVP 能力全部有真实实现和验收证据。
- “第一版暂时不做”的能力没有被偷偷并入 MVP。

### 3.2 数据闭环

```text
项目与成员
→ 集、场次、镜头和三类资产
→ 分配制作人并创建唯一视频或图片任务
→ 线下制作后上传文件
→ 自动创建不可覆盖版本、规范业务文件名和单版本审核单
→ 对指定版本批注
→ 退回或通过
→ 退回后生成下一版本，或确认最终版本并完成任务
→ 可选加入人工批量审核单
→ 刷新后仍可恢复完整状态
```

### 3.3 工程质量

- 前端技术栈和工程组织参考 `ruoyi-fastapi-frontend`。
- 后端遵循现有 Controller → Service → DAO → DO/VO 分层。
- PostgreSQL 结构变更同时通过 SQLAlchemy DO、Alembic 迁移和 `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql` 交付。
- 前端和后端都存在可重复执行的检查及测试命令。
- 核心链路存在真实集成测试和端到端测试。
- 生产配置不包含示例密码、私钥、Token 或默认密钥。

### 3.4 交付边界

以下结果不能单独作为项目完成证据：

- 静态原型或设计稿；
- Mock 数据可以操作；
- Swagger 中存在接口；
- 数据表已经建立；
- 前端构建成功；
- 服务健康检查成功；
- 只完成单个角色或单条成功路径；
- 只在不刷新页面时看起来正常。

## 4. 固定技术与产品决策

在用户没有明确变更前，以下决策作为计划基线：

- 业务前端是独立应用，不取代 `ruoyi-fastapi-frontend` 管理后台。
- 前端使用 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus、Sass 和 npm。
- 依赖版本、构建配置、认证和请求基础设施优先参考 `ruoyi-fastapi-frontend`。
- 后端复用现有 RuoYi FastAPI 服务，不另建第二套认证服务。
- 用户复用 `sys_user`，项目内身份通过项目成员关系表达。
- Shot Grid 新业务默认仅支持 PostgreSQL。
- 业务表使用 `sg_` 前缀。
- 业务 API 使用 `/shot-grid` 前缀。
- 权限码使用 `shotgrid:<resource>:<action>` 格式。
- 文件使用平台受保护文件和 `fileId` 引用机制。
- MVP 不交付客户独立门户；外部审核只保留未来兼容设计。

如果正式实现采用不同的 API 前缀、权限码或表名，必须先形成明确决策，并同步更新 `AGENTS.md`、本计划和接口文档。

## 5. 总体工作流

```text
阶段 0：契约与架构冻结
    ↓
阶段 1：应用基座
    ↓
阶段 2：PostgreSQL 与后端领域基础
    ↓
阶段 3：项目、集、场次、镜头、资产
    ↓
阶段 4：唯一制作任务与分配
    ↓
阶段 5：版本、文件与媒体
    ↓
阶段 6：批注与审核状态机
    ↓
阶段 7：审核单、工作台、搜索与文件页
    ↓
阶段 8：联调、质量门禁与发布
```

阶段之间允许在契约稳定后并行开发前端和后端，但不得让前端临时字段成为后端数据模型的事实标准。

## 6. 阶段 0：契约与架构冻结

### 目标

在创建大量页面之前，解决会影响所有模块的数据、权限、路由和媒体问题。

### 工作项

- [ ] 阅读并确认 `docs/参考项目评估.md` 的“采用、重做、放弃”清单，所有参考能力仍以当前契约重新设计。
- [x] 确认正式产品采用独立 `shot-grid-frontend`，系统管理保留在 `ruoyi-fastapi-frontend`。
- [ ] 确认前端脚手架从 `ruoyi-fastapi-frontend` 复用的文件清单。
- [ ] 明确业务应用登录方式以及与管理端是否共享登录态。
- [x] 确认业务路由使用本地白名单组件映射，并由 `/shot-grid/navigation` 返回应用范围菜单。
- [x] 定义项目角色：项目总监、制作人员，以及管理员的全局能力边界。
- [ ] 冻结项目、集、场次、镜头、资产、任务、版本、审核意见和审核单的字段字典。
- [x] 冻结项目文件缩写、制作人缩写和两类产出物文件命名规则。
- [ ] 冻结任务、版本、镜头和资产的状态代码及允许流转。
- [ ] 确认图片和视频的格式、大小上限、存储位置和保留策略。
- [ ] 确认缩略图、视频代理和媒体元数据由哪个后台任务处理。
- [ ] 冻结批注协议：毫秒时间点、归一化坐标、画笔/箭头/文字数据结构。
- [ ] 冻结统一分页、排序、筛选、错误码和并发冲突响应。
- [x] 冻结当前镜头 Excel 样表结构：多 Sheet 表示多集、A:P 主表、“序→000”、镜头号集内唯一和焦段文本。
- [ ] 冻结资产 Excel 最终样表，以及两类模板的版本号、最大行数、文件大小、名称规范化和原文件保留期限。
- [x] 冻结镜头先导、资产后导的 `sg_import_batch` 与 `sg_shot_asset_requirement` 数据关系、状态和 API 动作。
- [ ] 明确 NAS 路径只提供查看/复制，还是已有桌面协议处理器。
- [ ] 建立接口清单和最小 OpenAPI 契约。
- [ ] 建立关键决策记录，记录已选方案、未选方案和原因。

### 阶段出口

- 前后端字段名称、状态代码和主键类型一致。
- 文件上传、媒体播放、批注和审核动作有明确请求与响应契约。
- 不存在需要页面组件自行猜测的数据关系。
- 未决策项都有负责人或明确阻塞说明。

## 7. 阶段 1：应用基座

### 目标

建立可独立运行、可登录、可测试的业务前端骨架。

### 前端工作项

- [ ] 参考 `ruoyi-fastapi-frontend` 初始化 Vue 3 + Vite 工程。
- [ ] 建立并提交 `package.json` 和 `package-lock.json`。
- [ ] 配置开发、测试、生产环境变量和后端代理。
- [ ] 适配统一 Axios、Token、401、错误码、传输加密和下载逻辑。
- [ ] 适配用户信息、退出登录和权限加载。
- [ ] 建立业务端独立 Layout。
- [ ] 建立六个一级导航及占位路由。
- [ ] 建立 401、403、404 和通用异常页面。
- [ ] 建立全局加载、空状态、失败状态和消息反馈规范。
- [ ] 建立基础主题、状态颜色和响应式布局令牌。
- [ ] 添加 `lint`、`test`、`build:prod` 脚本。
- [ ] 建立第一批路由守卫、请求层和状态映射测试。

### 后端配合

- [ ] 确认 `/login` 和 `/getInfo` 能被业务应用安全复用。
- [x] 实现 `/shot-grid/navigation`，只返回 `route_name='ShotGrid'` 根节点下当前用户有权访问的白名单路由键。
- [ ] 配置业务端需要的 CORS、代理和传输加密路径。

### 阶段出口

- 使用真实后端账号完成登录、获取用户信息和退出。
- 未登录访问业务路由会被正确拦截。
- 无权限页面显示 403，不伪装成 404 或空数据。
- 刷新任意可访问路由后会恢复登录与路由状态。
- `npm.cmd run lint`、`npm.cmd run test` 和 `npm.cmd run build:prod` 可重复执行。

## 8. 阶段 2：PostgreSQL 与后端领域基础

### 目标

建立可靠的数据模型、权限基础和业务 API 骨架。

### 数据库工作项

- [x] 创建 `sg_project`。
- [x] 创建 `sg_project_member` 并关联 `sys_user`。
- [x] 创建 `sg_episode`。
- [x] 创建 `sg_scene`。
- [x] 创建 `sg_shot`。
- [x] 创建 `sg_asset`。
- [x] 创建 `sg_asset_item`。
- [x] 创建 `sg_shot_asset`。
- [x] 创建 `sg_task`。
- [x] 创建 `sg_version`。
- [x] 创建 `sg_version_file` 并关联 `sys_file_info`。
- [ ] 在版本提交 Service 中与 `sys_file_reference` 同事务维护业务引用。
- [x] 创建 `sg_note`。
- [x] 创建不可覆盖回复历史 `sg_note_reply`。
- [x] 创建 `sg_review_action`。
- [x] 创建 `sg_review_list`。
- [x] 创建 `sg_review_list_version`。
- [x] 创建 NAS 根目录白名单 `sg_storage_root` 和项目存储绑定 `sg_project_storage`。
- [x] 创建目录 Outbox/租约表 `sg_storage_operation`。
- [x] 创建版本暂存与 NAS 发布编排表 `sg_version_submission`。
- [x] 创建镜头与资产导入批次表 `sg_import_batch`。
- [x] 创建镜头先导入后的资产需求表 `sg_shot_asset_requirement`。
- [x] 为集号、场次号、镜头号、单目标唯一任务、版本序号、自动审核单和审核单顺序添加数据库约束。
- [x] 为资产规范化名称、导入幂等键和镜头资产需求匹配键添加数据库约束。
- [x] 为资产制作分项建立可空导入字段和非空名称部分唯一索引。
- [ ] 在版本提交 Service 中校验制作分项必填，并禁止已有版本的分项被普通编辑。
- [x] 为项目、状态、负责人、截止日期、任务和审核字段建立必要索引。
- [x] 提供 PostgreSQL Alembic 升级和降级迁移。
- [x] 同步更新 `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql` 初始化基线。

### 后端工作项

- [x] 建立独立 Shot Grid 业务模块及 Controller、Service、DAO、DO、VO 分层骨架。
- [x] 建立 `/shot-grid` 路由前缀。
- [x] 建立 `shotgrid:<resource>:<action>` 权限码。
- [x] 建立 Shot Grid 六项菜单、按钮权限和项目/画幅/资产类型等字典种子。
- [x] 实现项目成员和项目角色权限依赖。
- [ ] 在各资源 Service/DAO 中补齐资源归属校验。
- [ ] 在任务动作接口中补齐平台权限、项目角色和任务负责人三层授权，并保留项目成员只读协作能力。
- [x] 建立统一分页、排序方向和乐观锁请求基础模型。
- [ ] 在各资源查询 VO 中建立筛选字段与排序字段白名单。
- [x] 为可普通编辑的主数据 DO 统一审计字段、乐观锁和逻辑删除字段；历史、关系和编排表按契约使用专用字段。
- [ ] 在业务动作接口中接入操作日志。
- [ ] 实现基础健康或诊断信息，但不以此替代业务测试。

### 测试工作项

- [ ] 建立完整 RuoYi 平台 Alembic baseline，使 Alembic 可从真正空库独立创建全部平台表；当前 Shot Grid revision 是已有平台库上的增量迁移。
- [x] 更新后的 PostgreSQL 初始化 SQL 可从新库建立当前全量结构并写入 Alembic head。
- [x] 迁移在已有基线数据库上可执行升级、降级和再次升级。
- [x] 初始化 SQL 建库结果与 Alembic 最新版本的 Shot Grid 表、约束和索引一致。
- [x] 数据库约束拒绝无归属任务、同目标重复任务、重复版本号、重复自动审核单、重复审核单顺序和非法主文件角色。
- [ ] 非项目成员无法读取项目数据。
- [ ] DAO 和 Service 的事务回滚路径有测试。

### 阶段出口

- 数据模型通过迁移建立，不依赖 `create_all()`。
- OpenAPI 中能够识别 Shot Grid 接口和鉴权要求。
- 项目级数据权限在后端生效。
- 关键数据库不变量由约束或服务层测试证明。

## 9. 阶段 3：项目、集、场次、镜头与资产

### 目标

完成项目生产资料的基础录入和组织能力。

### 工作项

- [ ] 项目创建、编辑、列表、详情和归档。
- [ ] 项目成员添加、移除和项目角色调整。
- [ ] 项目代号直接作为唯一业务文件名前缀，制作人员保存项目内唯一制作人缩写；不再维护独立 `file_prefix`。
- [ ] 集创建、编辑、排序和项目内查询。
- [ ] 场次创建、编辑、排序和集内查询。
- [ ] 镜头创建、编辑、列表和详情。
- [ ] 镜头业务界面按契约提供字段、资产关联和只读聚合数据；界面列与 Excel 导入列保持明确边界。
- [ ] 镜头表服务端分页、搜索、筛选和排序。
- [ ] 镜头表格、卡片和故事板共用同一查询模型。
- [ ] 资产创建、编辑、列表和详情。
- [ ] 资产类型只实现 `Character`、`Environment`、`Prop`。
- [ ] 资产表按资产父级与制作分项层级提供类型、名称、描述、任务、状态和单一主制作人聚合数据。
- [ ] 镜头与资产多对多关系。
- [ ] 按真实样表实现多 Sheet 镜头导入：Sheet 名派生集，A:P 固定表头，忽略只读列与空白分隔后的辅助区。
- [ ] 镜头导入支持“序→000”、集内镜头号唯一、焦段文本无损保留，并按去重键统计和创建集、场次、镜头。
- [ ] 镜头样表中的场景资产未建立时只形成 `Environment` 待匹配需求，不从文本猜测角色或道具。
- [ ] Excel 资产导入模板、三类资产统计、预检查、错误报告和正式导入。
- [ ] 资产导入后唯一自动匹配镜头需求，并提供冲突人工处理、忽略和重新匹配。
- [ ] 项目概况统计使用真实聚合数据。

### 阶段出口

- 项目总监可以创建项目、添加成员，按顺序导入镜头表和资产表。
- 导入错误能定位到行和字段，不产生半成功脏数据。
- 镜头导入正确创建去重的集、场次和镜头；资产不存在时形成待匹配需求，不产生假资产。
- 资产导入能按三种类型统计新增数量，唯一匹配自动关联镜头，冲突留在人工处理列表。
- 表格、卡片和故事板切换后筛选条件与数据一致。
- 制作人员只能访问其所属项目。
- 刷新和重新登录后数据保持正确。

## 10. 阶段 4：唯一制作任务与分配

### 目标

完成镜头视频任务和资产图片任务的分配与状态链。

### 工作项

- [ ] 镜头首次分配制作人时创建唯一 `shot_video` 任务。
- [ ] 每个资产制作分项首次分配一名主制作人时创建唯一 `asset_image` 任务；复合制作人值拒绝导入。
- [ ] 制作分项缺失时只警告并允许落库和后续补充，但版本提交前必须补齐。
- [ ] 后续改派更新同一任务并记录审计，不重复创建正式任务。
- [ ] 支持任务负责人、优先级、截止日期和制作要求。
- [ ] 建立任务详情，聚合所属对象、制作资料、版本和意见。
- [ ] 明确平台不执行图片或视频制作，只承接线下产出物上传和审核。
- [ ] 建立工作台所需的“我的任务”“今日截止”“待审核”“修改中”等查询。
- [ ] 建立任务状态流转和权限校验。

### 阶段出口

- 未分配目标没有空负责人任务，首次分配后只生成一个正确类型的任务。
- 任务只能属于一个镜头或一个资产制作分项。
- 制作人员只能修改被授权任务，项目总监可以分配任务。

## 11. 阶段 5：版本、文件与媒体

### 目标

完成不可覆盖的版本历史和可审核媒体链路。

### 文件工作项

- [ ] 复用受保护文件上传和 `fileId` 引用。
- [ ] 定义主审核文件和可选辅助文件的用途字段；当前任务提交只强制一个符合类型约束的主产出物。
- [ ] 版本可关联主审核文件；缩略图、代理文件和源文件属于经确认后再实现的辅助能力，不得阻塞主上传审核闭环。
- [ ] 镜头任务只允许 MP4/MOV，资产任务只允许 JPG/PNG，并校验真实内容类型。
- [ ] 平台物理文件名保持不变，规范业务文件名保存到版本文件关系。
- [ ] Shot Grid 授权下载按业务文件名设置下载名称。
- [ ] 文件先以临时名发布到 NAS，校验大小和 SHA-256 后原子改名；数据库事务内不执行 NAS I/O。
- [ ] NAS 发布完成后，在短事务中保持文件引用、正式版本、业务文件名、自动审核单、任务状态和提交状态一致。
- [ ] 文件仍被版本引用时禁止直接永久删除。
- [ ] NAS 路径经过权限校验并安全展示、复制。

### 版本工作项

- [ ] 后端锁定任务并通过 `sg_version_submission` 保留 V001、V002 等序号；重试复用原序号、时间戳和业务文件名。
- [ ] 镜头按“项目缩写、EP 集、场次、S 镜头、制作人缩写、版本号、服务端时间戳”生成业务文件名；资产按类型、名称、制作分项、制作人、版本号和时间戳命名。
- [ ] 镜头名称严格满足 `{项目缩写}_EP{集号至少3位}_{场次号至少3位}_S{镜头号至少3位}_{制作人缩写}_V{版本号至少3位}_{服务端毫秒时间戳}.{mp4|mov}`。
- [ ] 资产名称严格满足 `{项目缩写}_Asset_{Character|Environment|Prop}_{资产名称}_{制作分项}_{制作人缩写}_V{版本号至少3位}_{服务端毫秒时间戳}.{jpg|png}`。
- [ ] 每次提交创建新版本，禁止覆盖旧版本。
- [ ] 记录提交人、提交时间和修改说明。
- [ ] 每次版本提交自动创建一个只包含当前版本的审核单。
- [ ] 支持图片和视频 AI 生成参数。
- [ ] AI 参数默认折叠并受权限控制。
- [ ] 支持版本历史和前后版本切换。

### 媒体工作项

- [ ] 提取图片和视频基础元数据。
- [ ] 生成缩略图。
- [ ] 根据冻结方案生成网页审核代理文件。
- [ ] 媒体处理具有排队、处理中、成功、失败状态。
- [ ] 图片预览和视频 Range 播放使用受保护接口。
- [ ] 媒体失败时显示明确错误，不使用永久占位假装成功。
- [ ] 页面卸载释放 Blob URL 和播放器资源。

### 阶段出口

- 上传文件、NAS 发布和正式版本短事务全部完成后才真实形成 V001、规范业务文件名和自动审核单；失败不得显示为版本成功，再次提交形成 V002 及新审核单。
- V001 的文件、参数和审核记录不会被 V002 覆盖。
- 图片可以预览，视频可以拖动并通过 Range 请求播放。
- 无权用户不能直接通过 URL 下载媒体。
- 超限文件、格式错误、处理失败和 Range 错误有明确反馈。
- 刷新后仍能查看版本历史与媒体处理状态。

## 12. 阶段 6：批注与审核状态机

### 目标

完成版本级审核、反馈和最终版本控制。

### 批注工作项

- [ ] 文字审核意见。
- [ ] 视频毫秒时间点批注。
- [ ] 图片和视频画圈批注。
- [ ] 箭头批注。
- [ ] 画面文字批注。
- [ ] 参考图片附件。
- [ ] 是否必须修改和处理状态。
- [ ] 制作人员回复。
- [ ] 批注草稿与正式提交分离。
- [ ] 切换版本时防止把意见提交到旧版本。

### 审核工作项

- [ ] 确认通过并直接形成最终版本、完成任务。
- [ ] 退回修改。
- [ ] 稍后决定。
- [ ] 审核动作权限校验。
- [ ] 审核动作审计。
- [ ] 最终版本唯一性。
- [ ] 退回后任务进入修改中，再上传生成新版本和新审核单。
- [ ] 并发审核冲突返回 409 并提示刷新。
- [ ] 版本状态与任务聚合状态保持一致。

### 前端工作项

- [ ] 审核页左侧版本列表。
- [ ] 中间图片/视频播放器。
- [ ] 版本切换和必要的 A/B 对比。
- [ ] 右侧当前及历史意见。
- [ ] 底部确认通过、退回修改、稍后决定三个审核动作。
- [ ] 时间点跳转和标注图层正确缩放。

### 阶段出口

- 审核意见始终绑定正确版本。
- 视频时间点和画面标注在不同显示尺寸下保持位置正确。
- 退回后可以再次提交新版本。
- 两人并发设置最终版本时数据库仍只有一个最终结果。
- 制作人员不能执行项目总监审核动作。
- 无权用户的响应中不包含隐藏的提示词、费用或内部讨论。

## 13. 阶段 7：审核单、工作台、搜索与文件页

### 目标

完成跨对象组织、集中审核和六个一级页面。

### 审核单

- [ ] 查看每次版本提交自动生成的单版本审核单。
- [ ] 创建、编辑和归档人工批量审核单。
- [ ] 添加和移除版本。
- [ ] 拖动调整版本顺序并持久化。
- [ ] 连续播放或连续查看。
- [ ] 从审核单进入单版本审核。
- [ ] 保留版本原始任务和对象归属。

### 工作台

- [ ] 我的制作任务。
- [ ] 我的待修改任务。
- [ ] 待我审核。
- [ ] 今日截止。
- [ ] 修改中任务。
- [ ] 最近提交。
- [ ] 每个区块能够进入真实对象或任务详情。

### 搜索与文件

- [ ] 按权限搜索镜头、资产和文件。
- [ ] 文件按项目、镜头、资产、任务和版本追溯。
- [ ] 查看文件版本和所属业务对象。
- [ ] 下载原文件。
- [ ] 查看和复制 NAS 路径。
- [ ] 禁止展示无业务归属文件。

### 阶段出口

- 审核单顺序刷新后保持不变。
- 工作台数据来自真实权限范围和状态。
- 搜索不会返回其他项目或无权资源。
- 六个一级页面和项目内部页面均不存在纯占位功能。

## 14. 阶段 8：联调、质量门禁与发布

### 功能联调

- [ ] 前端、后端、PostgreSQL 和 Redis 使用正式开发配置联调。
- [ ] 文件上传、业务引用、下载、Range 播放和媒体处理联调。
- [ ] 权限码、项目成员权限和数据范围联调。
- [ ] 刷新、重新登录、项目切换和异常恢复联调。

### 自动化测试

- [ ] 前端状态映射、批注坐标、时间格式和纯函数单元测试。
- [ ] 路由守卫、Pinia 和核心组件测试。
- [ ] 后端 Service、权限、事务和状态机测试。
- [ ] PostgreSQL 迁移测试。
- [ ] API 集成测试。
- [ ] 关键链路真实 E2E。

### 安全与隐私

- [ ] 项目成员越权测试。
- [ ] 资源 ID 枚举和跨项目引用测试。
- [ ] 文件名、评论、提示词和项目描述 XSS 测试。
- [ ] 私有文件直接 URL 访问测试。
- [ ] Token 过期和权限变更后的缓存失效测试。
- [ ] 日志、响应和前端状态敏感信息检查。
- [ ] 生产 CORS、密钥、数据库和 Redis 配置检查。

### 性能与可靠性

- [ ] 镜头和资产长列表分页或虚拟化验证。
- [ ] 搜索防抖和过期请求取消验证。
- [ ] 大媒体上传失败与重试验证。
- [ ] 视频拖动和 Range 播放验证。
- [ ] 媒体对象和 Blob URL 内存释放验证。
- [ ] 并发版本提交和并发审核验证。
- [ ] PostgreSQL 慢查询和必要索引检查。

### 发布资料

- [ ] 本地开发说明。
- [ ] 环境变量说明。
- [ ] PostgreSQL 迁移说明。
- [ ] 媒体处理依赖和部署说明。
- [ ] 备份、恢复和文件存储说明。
- [ ] 常见故障排查说明。
- [ ] 权限码和角色矩阵。
- [ ] API 契约或 OpenAPI 快照。
- [ ] 发布检查清单和回滚方案。

### 最终出口

- 所有 MVP 映射项完成。
- 自动化检查通过。
- 真实 E2E 通过。
- 不存在阻断级安全问题。
- 生产部署和回滚步骤可重复执行。
- 未完成项被明确标注为非 MVP 或阻塞项，没有用 Mock、手工改库或临时跳过掩盖。

## 15. MVP 需求映射

| 需求 | 主要阶段 | 完成证据 |
| --- | --- | --- |
| 创建项目 | 阶段 3 | 真实 PostgreSQL CRUD、项目权限测试 |
| 导入镜头表 | 阶段 3 | 模板、预检查、错误报告、事务导入测试 |
| 导入资产表并匹配镜头需求 | 阶段 3 | 类型统计、事务导入、唯一自动匹配、冲突人工处理和幂等测试 |
| 创建角色、场景、道具资产 | 阶段 3 | 资产 CRUD 和类型筛选 |
| 自动生成任务 | 阶段 4 | 首次分配生成单目标唯一任务测试 |
| 分配任务给成员 | 阶段 4 | 成员选择、权限和工作台验证 |
| 上传图片和视频版本 | 阶段 5 | 受保护上传、格式校验、两类规范业务文件名、文件引用、V001/V002、自动审核单 |
| 视频在线播放 | 阶段 5 | 鉴权 Range 播放和拖动验证 |
| 图片和视频批注 | 阶段 6 | 坐标、时间点和缩放恢复测试 |
| 审核通过与退回 | 阶段 6 | 状态机、权限、事务和审计 |
| 查看历史版本 | 阶段 5 | 不可覆盖版本和刷新恢复 |
| 创建审核单 | 阶段 5、7 | 自动单版本审核单、人工批量审核单、有序版本关系和连续审核 |
| 关联 NAS 文件路径 | 阶段 5、7 | 权限校验、查看和复制路径 |
| 搜索镜头、资产和文件 | 阶段 7 | 项目范围搜索和越权测试 |
| 根据权限显示内容 | 阶段 2—8 | 后端授权、响应裁剪和前端展示 |

## 16. 关键风险与控制措施

| 风险 | 影响 | 控制措施 |
| --- | --- | --- |
| 先做全量页面、后定接口 | 字段和状态反复返工 | 阶段 0 先冻结契约 |
| 直接复用全部 `/getRouters` | 业务端加载管理端菜单失败或越权 | 使用 `/shot-grid/navigation` 范围菜单和本地路由白名单 |
| 直接使用代码生成器的 CRUD 完成核心流程 | 绕过项目权限、状态机和跨表事务 | 生成器只产出骨架，核心 Service、状态机和测试人工实现 |
| 复用现有用户 Excel 直接落库模式 | 多表导入半成功且无法预览冲突 | 使用批次、preview/commit、Redis Token、全事务和幂等 |
| 镜头导入时隐式创建资产 | 拼写错误产生重复正式资产和错误任务 | 先建立待匹配需求，资产后导入只匹配唯一候选 |
| 重建用户表 | 两套账号和权限漂移 | 强制复用 `sys_user` |
| 文件只保存 URL | URL 失效且无法治理引用 | 保存 `fileId` 并复用业务引用 |
| 大视频直接上传后同步处理 | 请求超时、资源耗尽 | 冻结大小限制并采用异步媒体处理 |
| 前端生成版本号 | 并发出现重复版本 | 后端事务生成并添加唯一约束 |
| 标注保存像素坐标 | 播放器缩放后位置漂移 | 使用归一化坐标和自然尺寸 |
| 状态只在前端流转 | 可绕过权限和流程 | 后端状态机、事务和审计 |
| NAS 路径直接 `window.open` | 浏览器拦截或安全风险 | MVP 查看/复制，桌面协议另立项目 |
| 外部审核提前进入 MVP | 匿名授权和数据隔离扩大范围 | 当前只交付内部审核 |
| PostgreSQL 迁移链与初始化 SQL 不一致 | 新环境和升级环境结构漂移 | 每个结构变更同时提交并核对 Alembic 与 PostgreSQL 初始化 SQL |
| Mock 请求失败回退 | 掩盖真实后端不可用 | 生产代码禁止自动回退 Mock |

## 17. 阶段门禁

每个阶段完成时进行一次门禁评审：

1. 需求范围是否仍与 `需求.md` 一致。
2. 是否违反 `AGENTS.md` 的数据和权限不变量。
3. 前后端契约是否已同步。
4. PostgreSQL 迁移是否可重复执行。
5. 成功、失败、无权限和并发路径是否有验证。
6. 是否保留用户已有变更且没有提交敏感文件。
7. 实际验证结果是否与交付说明一致。

门禁未通过时，不得将该阶段标为完成。

## 18. 排期与负责人

本计划暂不填写虚假的日历工期。正式排期前需要确认：

- 前端、后端、测试和运维投入人数；
- 是否已有可复用的媒体转码服务；
- 文件大小和并发量级；
- 设计稿是否需要同步交付；
- 部署环境和对象存储/NAS 条件；
- MVP 是否需要真实外部用户参与验收。

确认后，为每个阶段补充：

| 阶段 | 负责人 | 计划开始 | 计划完成 | 当前状态 | 阻塞项 |
| --- | --- | --- | --- | --- | --- |
| 阶段 0 | 待定 | 待定 | 待定 | 进行中：主体契约已冻结 | 真实 Excel 参数、媒体与部署方案 |
| 阶段 1 | 待定 | 待定 | 待定 | 未开始 | 阶段 0 |
| 阶段 2 | 待定 | 2026-08-10 | 待定 | 进行中：数据库与后端骨架已完成 | 项目 CRUD、导入、版本与审核 Service |
| 阶段 3 | 待定 | 待定 | 待定 | 未开始 | 阶段 1、2 |
| 阶段 4 | 待定 | 待定 | 待定 | 未开始 | 阶段 3 |
| 阶段 5 | 待定 | 待定 | 待定 | 未开始 | 阶段 2、4、媒体方案 |
| 阶段 6 | 待定 | 待定 | 待定 | 未开始 | 阶段 5 |
| 阶段 7 | 待定 | 待定 | 待定 | 未开始 | 阶段 3—6 |
| 阶段 8 | 待定 | 待定 | 待定 | 未开始 | 阶段 1—7 |

## 19. 建议立即开始的工作

阶段 0 的架构、数据关系和 API 主体已冻结，阶段 2 的数据库与后端骨架已经建立。下一步先关闭仍影响编码的模板与部署参数，然后推进阶段 1 和阶段 3 的第一批真实业务闭环：

1. 获取并冻结镜头、资产真实 Excel 样表及模板版本。
2. 冻结导入最大行数、文件大小、名称规范化和原文件保留期限。
3. 确认同域不同路径部署、登录页复用和登录态共享方式。
4. 初始化独立业务前端工程，并接入真实登录、用户信息与 `/shot-grid/navigation`。
5. 实现项目创建事务、项目成员管理和项目范围查询，不通过通用 CRUD 绕过业务规则。
6. 实现镜头、资产 Excel 预检与提交 API，并加入模板版本、幂等和事务回滚测试。
7. 完成第一批可交付闭环：

```text
真实登录
→ 项目列表
→ 创建项目
→ 添加项目成员
→ 导入一个最小镜头表并生成集、场次、镜头和待匹配资产需求
→ 导入一个最小资产表并自动匹配唯一需求
→ 刷新后数据与权限保持正确
```

该闭环通过后，再进入任务、版本和审核链路。
