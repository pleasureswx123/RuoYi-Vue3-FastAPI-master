# Shot Grid 项目完成计划

> **2026-08-11 静态核对结论：**独立前端工程和后端 Shot Grid 业务源码均已形成，资源 CRUD、需求处理、任务、版本提交、审核单、审核动作、工作台、搜索及文件发现已有源码路径。当前结论统一为“源码完成，待集成/E2E”；本次未启动完整环境，不能表述为联调或验收通过。逐条状态见《[项目需求规格与业务规则](./项目需求规格与业务规则.md)》第 23 节。

## 1. 计划目的

本计划用于把《需求.md》中的 ShotGrid 轻量化产品方案交付为可运行、可验证、可继续扩展的 AI 影视项目管理系统。

计划覆盖：

- `shot-grid-frontend` 业务前端；
- `ruoyi-fastapi-backend` Shot Grid 业务接口；
- PostgreSQL 表结构与迁移；
- RuoYi 登录、用户、权限、文件和传输加密能力复用；
- 图片、视频、版本、批注和审核链路；
- 自动化测试、真实 E2E、部署和运维文档。

本计划不以“页面已经画完”作为完成标准。只有真实数据、权限、文件、状态流转和刷新恢复均通过验证，相关功能才算完成。

## 2. 当前状态

截至 2026-08-11 对前后端源码进行静态核对时：

- 已有产品需求：`docs/需求.md`；
- 已有开发约束：`../AGENTS.md`；
- 已建立并完成后端基座对齐、集层级、资产制作分项层级、单任务生产闭环、最新镜头/资产文件命名、独立业务端、范围导航和两类 Excel 导入设计的阶段 0 v1.2 契约：`docs/领域模型与API契约.md`；
- 已形成现有代码证据、能力复用边界和分阶段实施路线：`docs/若依基座分析与实施方案.md`；
- `shot-grid-frontend` 已有 Vue 3/Vite 可运行工程、`package.json`、业务 API、页面/组件与 Node 单元测试；`package-lock.json` 已提交，依赖锁定基线已进入版本控制；
- 后端 `module_shot_grid` 已具备项目编辑/归档、集/场次/镜头/资产/制作分项 CRUD、需求处理、任务分配/开始、版本提交与查询、审核单、意见/回复、`approve/reject/defer` 审核动作、工作台、搜索、文件发现和专用下载等源码；NAS Worker 也存在，但真实 PostgreSQL/Redis/NAS 集成与浏览器 E2E 均未验证；
- 当前主数据库为 PostgreSQL；
- RuoYi 已提供认证、用户、角色、权限、Redis、受保护文件和业务附件能力；
- 受保护图片/视频预览和批注组件、审核状态机源码已存在；媒体格式、大小、编码、分辨率、时长及“MVP 不生成代理”的 D-05 决策已冻结；服务端真实媒体校验和浏览器兼容性仍没有真实环境验收证据。

因此，项目已进入“主要源码成形、等待契约修正与真实集成/E2E”的状态。不得因为源码、单元测试、构建或页面可见而宣称 MVP 验收通过。

## 3. 完成定义

项目完成必须同时满足以下条件。

### 3.1 产品范围

- 六个一级页面可用：工作台、项目、镜头管理、资产库管理、版本审核、文件与 NAS。
- 项目内部页面可用：概况、集与场次、镜头、资产、审核、成员。
- 任务详情、镜头详情、资产详情和版本审核形成连贯业务路径。
- 《需求.md》列出的 14 项 MVP 能力全部有真实实现和验收证据。
- “第一版暂时不做”的能力没有被偷偷并入 MVP。

### 3.2 数据闭环

```text
项目与成员
→ 集、场次、镜头和三类资产
→ 分配制作人并创建唯一视频或图片任务
→ 线下制作后上传文件
→ 自动创建不可覆盖版本、规范业务文件名和单版本审核单
→ 对指定版本批注
→ 退回或通过
→ 退回后生成下一版本，或确认最终版本并完成任务
→ 可选加入人工批量审核单
→ 刷新后仍可恢复完整状态
```

### 3.3 工程质量

- 前端技术栈和工程组织参考 `ruoyi-fastapi-frontend`。
- 后端遵循现有 Controller → Service → DAO → DO/VO 分层。
- PostgreSQL 结构变更同时通过 SQLAlchemy DO、Alembic 迁移和 `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql` 交付。
- 前端和后端都存在可重复执行的检查及测试命令。
- 核心链路存在真实集成测试和端到端测试。
- 生产配置不包含示例密码、私钥、Token 或默认密钥。

### 3.4 交付边界

以下结果不能单独作为项目完成证据：

- 静态原型或设计稿；
- Mock 数据可以操作；
- Swagger 中存在接口；
- 数据表已经建立；
- 前端构建成功；
- 服务健康检查成功；
- 只完成单个角色或单条成功路径；
- 只在不刷新页面时看起来正常。

## 4. 固定技术与产品决策

在用户没有明确变更前，以下决策作为计划基线：

- 业务前端是独立应用，不取代 `ruoyi-fastapi-frontend` 管理后台。
- 前端使用 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus、Sass 和 npm。
- 依赖版本、构建配置、认证和请求基础设施优先参考 `ruoyi-fastapi-frontend`。
- 后端复用现有 RuoYi FastAPI 服务，不另建第二套认证服务。
- 用户复用 `sys_user`，项目内身份通过项目成员关系表达。
- Shot Grid 新业务默认仅支持 PostgreSQL。
- 业务表使用 `sg_` 前缀。
- 业务 API 使用 `/shot-grid` 前缀。
- 权限码使用 `shotgrid:<resource>:<action>` 格式。
- 文件使用平台受保护文件和 `fileId` 引用机制。
- MVP 不交付客户独立门户；外部审核只保留未来兼容设计。

如果正式实现采用不同的 API 前缀、权限码或表名，必须先形成明确决策，并同步更新 `AGENTS.md`、本计划和接口文档。

## 5. 总体工作流

```text
阶段 0：契约与架构冻结
    ↓
阶段 1：应用基座
    ↓
阶段 2：PostgreSQL 与后端领域基础
    ↓
阶段 3：项目、集、场次、镜头、资产
    ↓
阶段 4：唯一制作任务与分配
    ↓
阶段 5：版本、文件与媒体
    ↓
阶段 6：批注与审核状态机
    ↓
阶段 7：审核单、工作台、搜索与文件页
    ↓
阶段 8：联调、质量门禁与发布
```

阶段之间允许在契约稳定后并行开发前端和后端，但不得让前端临时字段成为后端数据模型的事实标准。

## 6. 阶段 0：契约与架构冻结

### 目标

在创建大量页面之前，解决会影响所有模块的数据、权限、路由和媒体问题。

### 工作项

- [ ] 阅读并确认 `docs/参考项目评估.md` 的“采用、重做、放弃”清单，所有参考能力仍以当前契约重新设计。
- [x] 确认正式产品采用独立 `shot-grid-frontend`，系统管理保留在 `ruoyi-fastapi-frontend`。
- [ ] 确认前端脚手架从 `ruoyi-fastapi-frontend` 复用的文件清单。
- [ ] 明确业务应用登录方式以及与管理端是否共享登录态。
- [x] 确认业务路由使用本地白名单组件映射，并由 `/shot-grid/navigation` 返回应用范围菜单。
- [x] 定义项目角色：项目总监、制作人员，以及管理员的全局能力边界。
- [ ] 冻结项目、集、场次、镜头、资产、任务、版本、审核意见和审核单的字段字典。
- [x] 冻结项目文件缩写、制作人缩写和两类产出物文件命名规则。
- [ ] 冻结任务、版本、镜头和资产的状态代码及允许流转。
- [x] 冻结 D-05 图片/视频格式、大小、编码、分辨率、时长及 MVP 不生成代理；存储与保留仍按文件/NAS 契约验收。
- [x] 冻结 MVP 不生成代理；缩略图未列为已交付能力，真实媒体元数据校验仍需补齐并联调。
- [ ] 冻结批注协议：毫秒时间点、归一化坐标、画笔/箭头/文字数据结构。
- [ ] 冻结统一分页、排序、筛选、错误码和并发冲突响应。
- [x] 冻结当前镜头 Excel 样表结构：多 Sheet 表示多集、A:P 主表、“序→000”、镜头号集内唯一和焦段文本。
- [x] 冻结资产 Excel 当前正式样表、两类模板版本、最大行数、文件大小、名称规范化和原文件仅临时解析不长期保留的边界。
- [x] 冻结镜头先导、资产后导的 `sg_import_batch` 与 `sg_shot_asset_requirement` 数据关系、状态和 API 动作。
- [ ] 明确 NAS 路径只提供查看/复制，还是已有桌面协议处理器。
- [ ] 建立接口清单和最小 OpenAPI 契约。
- [ ] 建立关键决策记录，记录已选方案、未选方案和原因。

### 阶段出口

- 前后端字段名称、状态代码和主键类型一致。
- 文件上传、媒体播放、批注和审核动作有明确请求与响应契约。
- 不存在需要页面组件自行猜测的数据关系。
- 未决策项都有负责人或明确阻塞说明。

## 7. 阶段 1：应用基座

### 目标

建立可独立运行、可登录、可测试的业务前端骨架。

### 前端工作项

- [x] 已初始化 Vue 3 + Vite 独立工程。
- [x] 已提交 `package.json` 和 `package-lock.json`，npm 依赖锁定基线已进入版本控制。
- [ ] 配置开发、测试、生产环境变量和后端代理。
- [ ] 适配统一 Axios、Token、401、错误码、传输加密和下载逻辑。
- [ ] 适配用户信息、退出登录和权限加载。
- [x] 已建立业务端独立 Layout。
- [x] 已建立六个一级导航和业务路由。
- [x] 已建立会话失效、403、404 和通用异常页面。
- [x] 已建立全局加载、空状态及错误页面组件。
- [ ] 建立基础主题、状态颜色和响应式布局令牌。
- [x] `package.json` 已提供 `lint`、`test`、`build:prod` 脚本。
- [x] 已建立路由守卫、请求层及业务工作区单元测试。

### 后端配合

- [ ] 确认 `/login` 和 `/getInfo` 能被业务应用安全复用。
- [x] 实现 `/shot-grid/navigation`，只返回 `route_name='ShotGrid'` 根节点下当前用户有权访问的白名单路由键。
- [ ] 配置业务端需要的 CORS、代理和传输加密路径。

### 阶段出口

- 使用真实后端账号完成登录、获取用户信息和退出。
- 未登录访问业务路由会被正确拦截。
- 无权限页面显示 403，不伪装成 404 或空数据。
- 刷新任意可访问路由后会恢复登录与路由状态。
- `npm.cmd run lint`、`npm.cmd run test` 和 `npm.cmd run build:prod` 可重复执行。

## 8. 阶段 2：PostgreSQL 与后端领域基础

### 目标

建立可靠的数据模型、权限基础和业务 API 骨架。

### 数据库工作项

- [x] 创建 `sg_project`。
- [x] 创建 `sg_project_member` 并关联 `sys_user`。
- [x] 创建 `sg_episode`。
- [x] 创建 `sg_scene`。
- [x] 创建 `sg_shot`。
- [x] 创建 `sg_asset`。
- [x] 创建 `sg_asset_item`。
- [x] 创建 `sg_shot_asset`。
- [x] 创建 `sg_task`。
- [x] 创建 `sg_version`。
- [x] 创建 `sg_version_file` 并关联 `sys_file_info`。
- [ ] 在版本提交 Service 中与 `sys_file_reference` 同事务维护业务引用。
- [x] 创建 `sg_note`。
- [x] 创建不可覆盖回复历史 `sg_note_reply`。
- [x] 创建 `sg_review_action`。
- [x] 创建 `sg_review_list`。
- [x] 创建 `sg_review_list_version`。
- [x] 创建 NAS 根目录白名单 `sg_storage_root` 和项目存储绑定 `sg_project_storage`。
- [x] 创建目录 Outbox/租约表 `sg_storage_operation`。
- [x] 创建版本暂存与 NAS 发布编排表 `sg_version_submission`。
- [x] 创建镜头与资产导入批次表 `sg_import_batch`。
- [x] 创建镜头先导入后的资产需求表 `sg_shot_asset_requirement`。
- [x] 为集号、场次号、镜头号、单目标唯一任务、版本序号、自动审核单和审核单顺序添加数据库约束。
- [x] 为资产规范化名称、导入幂等键和镜头资产需求匹配键添加数据库约束。
- [x] 为资产制作分项建立可空导入字段和非空名称部分唯一索引。
- [ ] 在版本提交 Service 中校验制作分项必填，并禁止已有版本的分项被普通编辑。
- [x] 为项目、状态、负责人、截止日期、任务和审核字段建立必要索引。
- [x] 提供 PostgreSQL Alembic 升级和降级迁移。
- [x] 同步更新 `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql` 初始化基线。

### 后端工作项

- [x] 建立独立 Shot Grid 业务模块及 Controller、Service、DAO、DO、VO 分层骨架。
- [x] 建立 `/shot-grid` 路由前缀。
- [x] 建立 `shotgrid:<resource>:<action>` 权限码。
- [x] 建立 Shot Grid 六项菜单、按钮权限和项目/画幅/资产类型等字典种子。
- [x] 实现项目成员和项目角色权限依赖。
- [ ] 在各资源 Service/DAO 中补齐资源归属校验。
- [ ] 在任务动作接口中补齐平台权限、项目角色和任务负责人三层授权，并保留项目成员只读协作能力。
- [x] 建立统一分页、排序方向和乐观锁请求基础模型。
- [ ] 在各资源查询 VO 中建立筛选字段与排序字段白名单。
- [x] 为可普通编辑的主数据 DO 统一审计字段、乐观锁和逻辑删除字段；历史、关系和编排表按契约使用专用字段。
- [x] 项目创建、成员动作和两类 Excel 正式提交已写入操作日志，创建与导入日志和领域数据同事务。
- [ ] 在后续普通 CRUD、任务、版本、审核和 NAS 运维动作中继续接入操作日志。
- [ ] 实现基础健康或诊断信息，但不以此替代业务测试。

### 测试工作项

- [ ] 建立完整 RuoYi 平台 Alembic baseline，使 Alembic 可从真正空库独立创建全部平台表；当前 Shot Grid revision 是已有平台库上的增量迁移。
- [x] 更新后的 PostgreSQL 初始化 SQL 可从新库建立当前全量结构并写入 Alembic head。
- [x] 迁移在已有基线数据库上可执行升级、降级和再次升级。
- [x] 初始化 SQL 建库结果与 Alembic 最新版本的 Shot Grid 表、约束和索引一致。
- [x] 数据库约束拒绝无归属任务、同目标重复任务、重复版本号、重复自动审核单、重复审核单顺序和非法主文件角色。
- [x] 项目访问 Service 的针对性测试证明非项目成员被稳定拒绝；完整浏览器越权 E2E 仍留在阶段 8。
- [x] 项目创建及两类 Excel 提交 Service 已覆盖针对性事务回滚测试；跨服务完整 E2E 仍留在阶段 8。

### 阶段出口

- 数据模型通过迁移建立，不依赖 `create_all()`。
- OpenAPI 中能够识别 Shot Grid 接口和鉴权要求。
- 项目级数据权限在后端生效。
- 关键数据库不变量由约束或服务层测试证明。

## 8.1 阶段 3—7 五维状态口径与验收命令

阶段 3—7 不再使用单一勾选框汇总能力。每项能力必须分别维护“后端源码、前端源码、自动化测试、真实联调、浏览器 E2E”：`完成/覆盖` 仅表示对应维度已有可定位证据，`部分完成` 必须写出缺口，`未验证` 不得折算为完成。表中的“负责人”是未完成维度的角色负责人，不虚构具体姓名；排期时须替换为实际人员。

表内命令缩写展开如下；`<case>` 替换为能力列中的 case 名。验收执行时必须保存命令、Git SHA、环境清单、脱敏日志、数据库/Redis/NAS 检查结果及浏览器截图/录像：

- `SG-BE`：`cd ruoyi-fastapi-backend && python -m pytest -q tests/module_shot_grid`。
- `SG-FE`：`cd shot-grid-frontend && npm ci && npm run lint && npm run test && npm run build:prod`。
- `SG-INT <case>`：`cd ruoyi-fastapi-test && python -m pytest -v -m shot_grid_integration -k "<case>"`；当前真实集成用例未建立或未执行时，该命令失败/无收集也只能记录为未完成，禁止改用单元测试冒充。
- `SG-E2E <case>`：`cd ruoyi-fastapi-test && python -m pytest -v -m shot_grid_e2e -k "<case>"`；必须连接真实浏览器和已启动的完整服务。
- `SG-NAS`：`cd ruoyi-fastapi-backend && python module_shot_grid/storage_worker.py --worker-id sg-storage-acceptance-01 --idle-seconds 1`，并严格执行 `docs/shot_grid_smb_acceptance.md` 的双 Worker、失锁接管、失败重试和对账矩阵。

证据目录当前是规定的交付位置，不代表证据已经存在；不存在完整证据包时，相应状态保持“未验证”。

## 9. 阶段 3：项目、集、场次、镜头与资产

### 五维状态表

| 能力 | 后端源码 | 前端源码 | 自动化测试 | 真实联调 | 浏览器 E2E | 未完成项负责人 | 依赖 | 验收命令 | 证据存放位置 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 项目创建、编辑、归档、概览与成员 | 完成 | 完成 | 后端覆盖 | 未验证 | 未验证 | 测试负责人（主责），前后端负责人配合 | PostgreSQL、Redis、真实账号、存储根 | `SG-BE`、`SG-INT project`、`SG-E2E project` | `docs/evidence/stage-3/project/` |
| 集 CRUD | 完成 | 部分完成：归档误用 `POST` | 后端契约覆盖 | 未验证 | 未验证 | 前端负责人（契约修正）；测试负责人（验收） | 项目能力；统一为后端 `PUT /archive` | `SG-FE`、`SG-INT episode`、`SG-E2E episode` | `docs/evidence/stage-3/episode/` |
| 场次 CRUD | 完成 | 部分完成：嵌套路径及归档 method 不一致 | 后端契约覆盖 | 未验证 | 未验证 | 前端负责人（契约修正）；测试负责人（验收） | 集 CRUD；冻结顶层 `/scenes` 契约 | `SG-FE`、`SG-INT scene`、`SG-E2E scene` | `docs/evidence/stage-3/scene/` |
| 镜头 CRUD、三视图与筛选 | 完成 | 部分完成：归档 method 不一致 | 前后端分段覆盖 | 未验证 | 未验证 | 前端负责人、测试负责人 | 集/场次契约、真实分页数据 | `SG-FE`、`SG-INT shot`、`SG-E2E shot` | `docs/evidence/stage-3/shot/` |
| 资产与制作分项 CRUD | 完成 | 部分完成：归档 method、`/asset-items` 路径不一致 | 后端契约覆盖 | 未验证 | 未验证 | 前端负责人、测试负责人 | 统一制作分项路径和详情行为 | `SG-FE`、`SG-INT asset`、`SG-E2E asset` | `docs/evidence/stage-3/asset/` |
| 镜头/资产 Excel 预检与提交 | 完成；模板下载端点未实现 | 完成（上传流程） | Parser/Controller/DAO/Service/安全覆盖 | 未验证 | 未验证 | 后端负责人（模板端点）；测试负责人（验收） | PostgreSQL、Redis、冻结样表 | `SG-BE`、`SG-INT import`、`SG-E2E import` | `docs/evidence/stage-3/import/` |
| 待匹配需求候选、绑定与关闭 | 完成 | 完成 | 契约覆盖 | 未验证 | 未验证 | 测试负责人 | 导入数据、资产 CRUD | `SG-BE`、`SG-FE`、`SG-INT requirement`、`SG-E2E requirement` | `docs/evidence/stage-3/requirement/` |

### 阶段出口

只有上述各行五维证据齐全，且权限、刷新恢复、错误定位、幂等与事务回滚均通过，阶段 3 才能完成；当前结论为 **源码主体完成，前端契约待修正，真实联调/E2E 未验证**。

## 10. 阶段 4：唯一制作任务与分配

### 五维状态表

| 能力 | 后端源码 | 前端源码 | 自动化测试 | 真实联调 | 浏览器 E2E | 未完成项负责人 | 依赖 | 验收命令 | 证据存放位置 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 镜头/制作分项首次分配与改派唯一任务 | 完成 | 完成 | Task Service 覆盖 | 未验证 | 未验证 | 测试负责人 | 阶段 3 资源契约、PostgreSQL 唯一约束、项目成员 | `SG-BE`、`SG-INT task-assignment`、`SG-E2E task-assignment` | `docs/evidence/stage-4/assignment/` |
| 任务详情、负责人、优先级、截止日期、制作要求 | 完成 | 完成 | Task Service/前端工作区分段覆盖 | 未验证 | 未验证 | 测试负责人 | 分配能力、真实成员权限 | `SG-BE`、`SG-FE`、`SG-INT task-detail`、`SG-E2E task-detail` | `docs/evidence/stage-4/detail/` |
| 开始任务、我的任务与状态权限 | 完成（仅当前具名动作） | 完成 | Task Service/Discovery 覆盖 | 未验证 | 未验证 | 测试负责人 | Redis 会话、平台权限、项目角色、任务负责人三层授权 | `SG-BE`、`SG-INT task-flow`、`SG-E2E task-flow` | `docs/evidence/stage-4/flow/` |

### 阶段出口

验证单目标唯一任务、负责人授权与项目总监分配权之前不得完成本阶段。重新打开、取消等未公开具名动作不属于当前已实现能力，不得从页面文案推断其存在。

## 11. 阶段 5：版本、文件与媒体

### 已冻结媒体边界

D-05 已于 2026-08-11 冻结：镜头视频仅 MP4/MOV、H.264/AVC、最大 2 GiB、3840×2160、600 秒；资产图片仅 JPEG/PNG、最大 50 MiB、8192×8192；MVP 不生成代理。前端扩展名、声明 MIME 与大小提示不是权威校验，真实内容、编码、尺寸、时长和浏览器兼容性仍须由服务端及受控样本联调证明。

### 五维状态表

| 能力 | 后端源码 | 前端源码 | 自动化测试 | 真实联调 | 浏览器 E2E | 未完成项负责人 | 依赖 | 验收命令 | 证据存放位置 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 受保护上传、`fileId`/业务引用与下载授权 | 完成 | 完成 | 文件引用失败回滚、下载授权契约覆盖 | 未验证 | 未验证 | 测试负责人、文件服务负责人 | PostgreSQL、Redis、平台私有文件服务、ACL 样本 | `SG-BE`、`SG-INT protected-file`、`SG-E2E protected-file` | `docs/evidence/stage-5/protected-file/` |
| 版本提交、不可覆盖历史、命名与自动审核单 | 完成 | 完成 | Service 事务覆盖 V001/V002、回滚、幂等 | 未验证 | 未验证 | 测试负责人 | 已开始任务、制作人缩写、文件服务、NAS 发布 | `SG-BE`、`SG-INT version`、`SG-E2E version` | `docs/evidence/stage-5/version/` |
| NAS 临时写入、SHA-256、原子发布、重试与对账 | 部分完成：Worker/重试/诊断源码存在，发布语义 SLA 待冻结 | 状态展示完成；仅查看/复制路径 | Worker 单测覆盖；受控 NAS 未覆盖 | 未验证 | 未验证 | 产品负责人（语义）、后端负责人、NAS 运维、测试负责人 | `SG-BLOCK-NAS-001`、受控 SMB、服务账号 ACL | `SG-BE`、`SG-NAS`、`SG-INT nas-publish`、`SG-E2E nas-publish` | `ruoyi-fastapi-backend/docs/evidence/shot-grid-smb/` 与 `docs/evidence/stage-5/nas/` |
| 媒体策略、真实校验、鉴权预览与 Range 播放 | 部分完成：文件访问存在，完整媒体探测/策略接口待核实补齐 | 预览组件完成 | 下载契约与媒体组件单测覆盖 | 未验证 | 未验证 | 后端负责人、媒体技术负责人、测试负责人 | 已冻结 D-05、受控合法/非法媒体样本、Range 文件服务 | `SG-BE`、`SG-FE`、`SG-INT media`、`SG-E2E media` | `docs/evidence/stage-5/media/` |
| 缩略图与代理生成 | 不适用：MVP 明确不生成代理；缩略图未承诺为完成能力 | 不得展示虚假处理成功 | 未覆盖 | 未验证 | 未验证 | 产品负责人（若扩展范围则重新立项） | 非 MVP；变更需先更新 D-05 | `SG-INT media` | `docs/evidence/stage-5/media/decision.md` |

### 阶段出口

仅当上传、真实媒体校验、受控 NAS 发布、短事务入库、ACL/Range、失败补偿和刷新恢复均有证据时，本阶段才可完成。源码或临时目录测试不能替代受控 SMB/NAS 验收。

## 12. 阶段 6：批注与审核状态机

### 五维状态表

| 能力 | 后端源码 | 前端源码 | 自动化测试 | 真实联调 | 浏览器 E2E | 未完成项负责人 | 依赖 | 验收命令 | 证据存放位置 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 文字/时间点/画圈/箭头/文字标注与附件 | 完成 | 完成 | Review VO/访问、前端批注覆盖 | 未验证 | 未验证 | 测试负责人 | 阶段 5 媒体预览、冻结批注协议、参考图片 ACL | `SG-BE`、`SG-FE`、`SG-INT annotation`、`SG-E2E annotation` | `docs/evidence/stage-6/annotation/` |
| 意见状态、回复、草稿隔离与版本切换保护 | 完成（意见/回复/状态） | 完成 | 前后端分段覆盖 | 未验证 | 未验证 | 前端负责人（草稿/切换验收）、测试负责人 | 多版本数据、并发/刷新场景 | `SG-FE`、`SG-INT note`、`SG-E2E note` | `docs/evidence/stage-6/note/` |
| `approve`/`reject`/`defer`、唯一 final、任务联动与审计 | 完成；审核动作闭环已实现 | 完成；三个动作入口已接入 | Service 事务覆盖并发冲突、V001 退回/V002 通过、审计 | 未验证 | 未验证 | 测试负责人 | PostgreSQL 真实行锁、director/creator 账号、阶段 5 版本链 | `SG-BE`、`SG-INT review-action`、`SG-E2E review-action` | `docs/evidence/stage-6/review-action/` |

### 阶段出口

后端审核动作及事务测试已经完成，但这不等于阶段验收。必须在真实 PostgreSQL 并发、真实角色权限和浏览器中证明批注缩放、时间跳转、退回再提交、唯一 final 与 409 刷新提示。

## 13. 阶段 7：审核单、工作台、搜索与文件页

### 五维状态表

| 能力 | 后端源码 | 前端源码 | 自动化测试 | 真实联调 | 浏览器 E2E | 未完成项负责人 | 依赖 | 验收命令 | 证据存放位置 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 自动/人工审核单、eligible versions、排序与归档 | 完成 | 完成 | Review List Service 覆盖 | 未验证 | 未验证 | 测试负责人 | 阶段 5/6 版本审核链、刷新验证数据 | `SG-BE`、`SG-INT review-list`、`SG-E2E review-list` | `docs/evidence/stage-7/review-list/` |
| 连续查看/播放并进入单版本审核 | 完成（数据接口） | 完成 | 前端/Service 分段覆盖 | 未验证 | 未验证 | 测试负责人 | 审核单、真实图片/视频、Range | `SG-FE`、`SG-INT review-playback`、`SG-E2E review-playback` | `docs/evidence/stage-7/review-playback/` |
| 工作台各状态区块与真实详情跳转 | 完成 | 完成 | Discovery 契约覆盖 | 未验证 | 未验证 | 测试负责人 | 任务/审核状态数据、不同角色账号 | `SG-BE`、`SG-FE`、`SG-INT workbench`、`SG-E2E workbench` | `docs/evidence/stage-7/workbench/` |
| 权限范围搜索与文件业务追溯、下载 | 完成 | 完成 | Discovery/下载授权契约覆盖 | 未验证 | 未验证 | 测试负责人 | 多项目隔离数据、文件 ACL、阶段 5 文件链 | `SG-BE`、`SG-INT discovery`、`SG-E2E discovery` | `docs/evidence/stage-7/discovery/` |
| NAS/UNC 路径查看与复制 | 完成（授权后返回路径） | 完成（查看/复制） | 分段覆盖 | 未验证 | 未验证 | NAS 运维、测试负责人 | 受控 SMB；项目成员权限；路径敏感性策略 | `SG-NAS`、`SG-INT nas-path`、`SG-E2E nas-path` | `docs/evidence/stage-7/nas-path/` |
| UNC 一键打开 | 未承诺 | 未实现（符合当前边界） | 不适用 | 未验证 | 未验证 | IT/NAS 运维负责人、安全负责人（如决定启用） | `SG-BLOCK-UNC-001`：确认桌面协议 scheme、部署范围、浏览器白名单与回退 | `SG-E2E unc-handler` | `docs/evidence/stage-7/unc-handler/` |

### 阶段出口

审核单顺序、工作台权限范围、搜索隔离、文件追溯及 NAS 路径查看/复制通过真实联调和浏览器验证后，阶段 7 才能完成。浏览器直接打开 UNC 不属于当前 MVP；只有部署并验证专用桌面协议处理器后才可另行启用。

## 14. 阶段 8：联调、质量门禁与发布

### 功能联调

- [ ] 前端、后端、PostgreSQL 和 Redis 使用正式开发配置联调。
- [ ] 文件上传、业务引用、下载、Range 播放和媒体处理联调。
- [ ] 权限码、项目成员权限和数据范围联调。
- [ ] 刷新、重新登录、项目切换和异常恢复联调。

### 自动化测试

- [ ] 前端状态映射、批注坐标、时间格式和纯函数单元测试。
- [ ] 路由守卫、Pinia 和核心组件测试。
- [ ] 后端 Service、权限、事务和状态机测试。
- [ ] PostgreSQL 迁移测试。
- [ ] API 集成测试。
- [ ] 关键链路真实 E2E。

### 安全与隐私

- [x] 两类 Excel 已覆盖 ZIP/OOXML 资源放大、隐藏 Sheet、单格与总文本、预览 JSON 大小的针对性门禁测试；真实网关压测仍待阶段 8。
- [ ] 项目成员越权测试。
- [ ] 资源 ID 枚举和跨项目引用测试。
- [ ] 文件名、评论、提示词和项目描述 XSS 测试。
- [ ] 私有文件直接 URL 访问测试。
- [ ] Token 过期和权限变更后的缓存失效测试。
- [ ] 日志、响应和前端状态敏感信息检查。
- [ ] 生产 CORS、密钥、数据库和 Redis 配置检查。

### 性能与可靠性

- [ ] 镜头和资产长列表分页或虚拟化验证。
- [ ] 搜索防抖和过期请求取消验证。
- [ ] 大媒体上传失败与重试验证。
- [ ] 视频拖动和 Range 播放验证。
- [ ] 媒体对象和 Blob URL 内存释放验证。
- [ ] 并发版本提交和并发审核验证。
- [ ] 资产工作簿重新保存、移动行或更改 Sheet 后重导的治理验证，重点覆盖多个未命名制作分项不得静默复制。
- [ ] PostgreSQL 慢查询和必要索引检查。

### 发布资料

- [ ] 本地开发说明。
- [ ] 环境变量说明。
- [ ] PostgreSQL 迁移说明。
- [ ] 媒体处理依赖和部署说明。
- [ ] 备份、恢复和文件存储说明。
- [ ] 常见故障排查说明。
- [ ] 权限码和角色矩阵。
- [ ] API 契约或 OpenAPI 快照。
- [ ] 发布检查清单和回滚方案。

### 最终出口

- 所有 MVP 映射项的五维状态均满足其验收口径。
- 自动化检查通过。
- **只有真实 PostgreSQL、Redis、平台文件服务、受控 NAS/SMB 和真实浏览器 E2E 五类环境证据全部齐备且可复现，才允许标记“MVP 验收通过”；缺少任一类时必须保持“未通过/未验证”。**
- 不存在阻断级安全问题。
- 生产部署和回滚步骤可重复执行。
- 未完成项被明确标注为非 MVP 或阻塞项，没有用 Mock、手工改库或临时跳过掩盖。

## 15. MVP 需求映射

| 需求 | 主要阶段 | 完成证据 |
| --- | --- | --- |
| 创建项目 | 阶段 3 | 真实 PostgreSQL CRUD、项目权限测试 |
| 导入镜头表 | 阶段 3 | 模板、预检查、错误报告、事务导入测试 |
| 导入资产表并匹配镜头需求 | 阶段 3 | 类型统计、事务导入、唯一自动匹配、冲突人工处理和幂等测试 |
| 创建角色、场景、道具资产 | 阶段 3 | 资产 CRUD 和类型筛选 |
| 自动生成任务 | 阶段 4 | 首次分配生成单目标唯一任务测试 |
| 分配任务给成员 | 阶段 4 | 成员选择、权限和工作台验证 |
| 上传图片和视频版本 | 阶段 5 | 受保护上传、格式校验、两类规范业务文件名、文件引用、V001/V002、自动审核单 |
| 视频在线播放 | 阶段 5 | 鉴权 Range 播放和拖动验证 |
| 图片和视频批注 | 阶段 6 | 坐标、时间点和缩放恢复测试 |
| 审核通过与退回 | 阶段 6 | 状态机、权限、事务和审计 |
| 查看历史版本 | 阶段 5 | 不可覆盖版本和刷新恢复 |
| 创建审核单 | 阶段 5、7 | 自动单版本审核单、人工批量审核单、有序版本关系和连续审核 |
| 关联 NAS 文件路径 | 阶段 5、7 | 权限校验、查看和复制路径 |
| 搜索镜头、资产和文件 | 阶段 7 | 项目范围搜索和越权测试 |
| 根据权限显示内容 | 阶段 2—8 | 后端授权、响应裁剪和前端展示 |

## 16. 关键风险与控制措施

| 风险 | 影响 | 控制措施 |
| --- | --- | --- |
| 先做全量页面、后定接口 | 字段和状态反复返工 | 阶段 0 先冻结契约 |
| 直接复用全部 `/getRouters` | 业务端加载管理端菜单失败或越权 | 使用 `/shot-grid/navigation` 范围菜单和本地路由白名单 |
| 直接使用代码生成器的 CRUD 完成核心流程 | 绕过项目权限、状态机和跨表事务 | 生成器只产出骨架，核心 Service、状态机和测试人工实现 |
| 复用现有用户 Excel 直接落库模式 | 多表导入半成功且无法预览冲突 | 使用批次、preview/commit、Redis Token、全事务和幂等 |
| 镜头导入时隐式创建资产 | 拼写错误产生重复正式资产和错误任务 | 先建立待匹配需求，资产后导入只匹配唯一候选 |
| 重建用户表 | 两套账号和权限漂移 | 强制复用 `sys_user` |
| 文件只保存 URL | URL 失效且无法治理引用 | 保存 `fileId` 并复用业务引用 |
| 大视频直接上传后同步处理 | 请求超时、资源耗尽 | 冻结大小限制并采用异步媒体处理 |
| 前端生成版本号 | 并发出现重复版本 | 后端事务生成并添加唯一约束 |
| 标注保存像素坐标 | 播放器缩放后位置漂移 | 使用归一化坐标和自然尺寸 |
| 状态只在前端流转 | 可绕过权限和流程 | 后端状态机、事务和审计 |
| NAS 路径直接 `window.open` | 浏览器拦截或安全风险 | MVP 查看/复制，桌面协议另立项目 |
| 外部审核提前进入 MVP | 匿名授权和数据隔离扩大范围 | 当前只交付内部审核 |
| PostgreSQL 迁移链与初始化 SQL 不一致 | 新环境和升级环境结构漂移 | 每个结构变更同时提交并核对 Alembic 与 PostgreSQL 初始化 SQL |
| 资产来源行键依赖文件摘要和物理坐标 | 工作簿重存或移动行后，未命名制作分项可能被当作新行 | 后续模板引入稳定 `rowUid`，或建立疑似重复提示与人工治理；当前不宣称跨文件逻辑幂等 |
| Mock 请求失败回退 | 掩盖真实后端不可用 | 生产代码禁止自动回退 Mock |

## 17. 阶段门禁

每个阶段完成时进行一次门禁评审：

1. 需求范围是否仍与 `需求.md` 一致。
2. 是否违反 `AGENTS.md` 的数据和权限不变量。
3. 前后端契约是否已同步。
4. PostgreSQL 迁移是否可重复执行。
5. 成功、失败、无权限和并发路径是否有验证。
6. 是否保留用户已有变更且没有提交敏感文件。
7. 实际验证结果是否与交付说明一致。

门禁未通过时，不得将该阶段标为完成。

## 18. 排期与负责人

本计划暂不填写虚假的日历工期。正式排期前需要确认：

- 前端、后端、测试和运维投入人数；
- 是否已有可复用的媒体转码服务；
- 文件大小和并发量级；
- 设计稿是否需要同步交付；
- 部署环境和对象存储/NAS 条件；
- MVP 是否需要真实外部用户参与验收。

确认后，为每个阶段补充：

| 阶段 | 负责人 | 计划开始 | 计划完成 | 当前状态 | 阻塞项 |
| --- | --- | --- | --- | --- | --- |
| 阶段 0 | 待定 | 待定 | 待定 | 进行中：主体契约、Excel 参数与 D-05 媒体限制已冻结 | NAS 发布 SLA、UNC 可选扩展与部署方案 |
| 阶段 1 | 待定 | 待定 | 待定 | 未开始 | 阶段 0 |
| 阶段 2 | 待定 | 2026-08-10 | 待定 | 后端领域源码主体完成 | 完整平台 Alembic baseline、真实联调/E2E |
| 阶段 3 | 待定 | 2026-08-10 | 待定 | 源码主体完成，前端资源契约部分不一致 | 修正路径/method、真实联调/E2E |
| 阶段 4 | 待定 | 待定 | 待定 | 前后端源码完成，待集成/E2E | 阶段 3 契约、真实权限/数据 |
| 阶段 5 | 待定 | 待定 | 待定 | 版本/文件/NAS 源码主体完成，媒体校验部分完成 | 真实文件服务、受控 NAS、D-05 样本验收 |
| 阶段 6 | 待定 | 待定 | 待定 | 批注与审核动作源码完成，待集成/E2E | 阶段 5、真实并发与角色 |
| 阶段 7 | 待定 | 待定 | 待定 | 审核单/工作台/搜索/文件源码完成，待集成/E2E | 阶段 3—6、NAS 路径验收 |
| 阶段 8 | 待定 | 待定 | 待定 | 未开始 | 阶段 1—7 |

## 19. 建议立即开始的工作

镜头、资产样表参数，项目创建/成员/范围查询，以及两类 Excel 预检与提交 API 已完成。下一步按以下顺序继续：

1. 冻结同域不同路径部署、登录页复用和登录态共享方式，并在真实环境验证登录恢复。
2. 修正集/场次/镜头/资产/制作分项的前后端路径与归档 HTTP method 差异。
3. 启动 PostgreSQL、Redis、FastAPI 和独立业务前端，按《第一批后端联调清单》完成资源、需求、任务、版本和审核接口联调。
4. 在受控 SMB/NAS 共享验证 Outbox Worker、版本原子发布、失败重试和对账，不以临时目录测试替代 NAS 验收。
5. 完成第一批浏览器可交付闭环：

```text
真实登录
→ 项目列表
→ 创建项目
→ 添加项目成员
→ 导入一个最小镜头表并生成集、场次、镜头和待匹配资产需求
→ 导入一个最小资产表并自动匹配唯一需求
→ 刷新后数据与权限保持正确
```

该闭环通过后，再进入任务、版本和审核链路。


## 20. 2026-08-11 静态核对与剩余门禁

### 20.1 五维状态结论

《项目需求规格与业务规则》第 23 节已为第 5—20 节需求分配稳定编号，并分别记录后端源码、前端源码、单元测试、真实联调、浏览器 E2E。当前真实联调和浏览器 E2E 均为“未验证”；已有源码能力的统一对外口径是 **源码完成，待集成/E2E**。

静态核对发现一项必须先修正的契约差异：后端资源归档统一注册为 `PUT /archive`，前端 `episodes.js`、`scenes.js`、`shots.js`、`assets.js` 当前发送 `POST /archive`。在修正并联调前，资源归档只能标为“前端部分完成”。

### 20.2 产品参数阻塞项

| 阻塞编号 | 产品参数 | 当前安全默认/影响 | 负责人 | 确认期限 | 未确认时门禁 |
| --- | --- | --- | --- | --- | --- |
| `SG-BLOCK-MEDIA-001`（已关闭） | D-05 媒体格式、大小、编码、分辨率、时长及代理策略 | 2026-08-11 已冻结；MVP 不生成代理。关闭决策阻塞不等于媒体实现或验收完成 | 产品负责人＋媒体技术负责人 | 已确认 | 后续只保留服务端真实校验、受控样本与浏览器兼容性验收门禁 |
| `SG-BLOCK-NAS-001` | NAS 发布是同步完成还是后台发布；“提交成功”“可审核”的准确时点及失败补偿 SLA | 维持“发布完成前不可进入待审核”，不展示虚假成功版本 | 产品负责人（主责）＋后端负责人/NAS 运维（会签） | 2026-08-18 | 阻塞 `SG-STORAGE-007`、`SG-VERSION-001` 和真实闭环验收 |
| `SG-BLOCK-UNC-001` | 公司是否部署统一 UNC/SMB 桌面协议处理器、支持的 scheme、浏览器白名单与失败回退 | 未确认前仅查看/复制路径，不提供“一键打开” | IT/NAS 运维负责人（主责）＋安全负责人/前端负责人（会签） | 2026-08-20 | 阻塞 UNC 直达按钮；不阻塞只读路径展示 |

负责人需在期限前把决定同步到需求基线、领域/API 契约和部署说明；聊天确认或临时环境行为不算冻结。
