# Shot Grid 项目完成计划

## 1. 计划目的

本计划用于把《需求.md》中的 ShotGrid 轻量化产品方案交付为可运行、可验证、可继续扩展的 AI 影视项目管理系统。

计划覆盖：

- `shot-grid-frontend` 业务前端；
- `ruoyi-fastapi-backend` Shot Grid 业务接口；
- PostgreSQL 表结构与迁移；
- RuoYi 登录、用户、权限、文件和传输加密能力复用；
- 图片、视频、版本、批注和审核链路；
- 自动化测试、真实 E2E、部署和运维文档。

本计划不以“页面已经画完”作为最终完成标准。快速迭代期间，代码实现完成与最终验收完成必须分开记录：功能可以在最小定向检查通过后进入下一项开发，但只有真实数据、权限、文件、状态流转和刷新恢复在阶段或最终门禁中通过验证，才可标记为最终验收完成。

## 2. 当前状态

2026-08-14 已按新的正式审核基线完成核心功能代码：修改问题永久绑定来源版本；制作人提交修订版时逐条填写处理说明；审核人在新版本逐条确认历史问题为 `resolved/still_present`，同时可以提出绑定当前版本的新问题；全部问题关闭后才允许通过。旧 `isMandatory + reply + resolve` 模型已移除；以下旧 `[x]` 仅保留为历史实现记录。

截至 2026-08-12 审核媒体预览、基础画面批注与 A/B 对比完成代码接入时：

- 已有产品需求：`docs/需求.md`；
- 已有开发约束：`../AGENTS.md`；
- 已建立并完成后端基座对齐、集层级、资产制作分项层级、单任务生产闭环、最新镜头/资产文件命名、独立业务端、范围导航、两类 Excel 导入和两个 Worker 设计的阶段 0 契约：`docs/领域模型与API契约.md`；
- 已形成现有代码证据、能力复用边界和分阶段实施路线：`docs/若依基座分析与实施方案.md`；
- `shot-grid-frontend` 已建立独立 Vue 3/Vite/Pinia/Vue Router/Axios/Element Plus 工程，已提交依赖锁文件，接入真实验证码、登录、`/getInfo`、退出和 `/shot-grid/navigation`，并建立统一请求、传输加密、业务 Layout、六项白名单路由和基础错误页；项目、镜头和资产一级页均已接入真实 API。工作台已接入真实 `/shot-grid/tasks/mine`，任务详情深链 `/tasks/:taskId` 已接入详情、开始、编辑、版本工作区和全部 open 修改问题；修订版提交逐条保存处理说明。版本审核详情已接入跨版本历史问题确认、当前版新问题、`approve/reject/defer`、鉴权媒体、视频时间点、四类批注和 A/B 对比。文件与 NAS 一级页已接入项目正式版本文件分页、受保护下载、NAS 相对路径、项目存储状态、UNC 安全快照、目录诊断与人工重试。既有浏览器证据不覆盖本次新闭环，本次由用户手动验收；
- 后端已建立 `module_shot_grid` 领域基础、`20260810_01 → 20260814_10` Alembic 迁移链和 PostgreSQL 初始化基线，并实现项目/成员/镜头/资产/任务/版本、导入、资产需求、人工批量审核、项目文件、默认关闭的目录/版本/媒体 Worker，以及跨版本修改问题、制作人逐条处理说明和审核人逐条确认事务；图片派生使用 Pillow，视频缩略图/H.264 MP4 代理使用显式配置的 FFmpeg；
- NAS 目录 Worker 与版本发布 Worker 在所有环境样例中均默认关闭，当前批次不包含正式 Windows Worker 服务账号、真实 NAS/AD/共享 ACL 和隔离 UNC 根目录 E2E；临时本地目录、Mock、单元测试或 Scheduler 注册均不能替代该部署验收；
- 当前主数据库为 PostgreSQL；
- RuoYi 已提供认证、用户、角色、权限、Redis、受保护文件和业务附件能力；
- Shot Grid 的后端版本意见、结构化视频时间点/归一化批注、自动单版与人工批量审核状态机已有实现；独立前端 Dockerfile 和 Nginx 模板已固定 `/shot-grid-app/` 页面路径与剥离 `/prod-api/` 的 API 代理，并已走通项目管理、镜头管理/镜头 Excel 导入、资产管理/资产 Excel 导入及任务工作台/版本上传四个隔离子集浏览器旅程。资产子集只验证真实缩略图空态；任务/版本子集只使用本地 TEMP 适配器发布图片文件，未验证权限撤权负向、Range 206/416 真分段、MP4/MOV、真实 Windows/NAS 服务账号或 UNC/SMB。审核页已经实现基础媒体预览、点/矩形/箭头/文字批注、时间定位、A/B 对比和人工批量审核，但当前通过统一鉴权请求完整取得 Blob，不能据此声称浏览器 Range 206/416 真分段播放已经完成。缩略图生成、媒体代理、codec/媒体轨与转码探测及完整系统 E2E 尚未实现或验收。

因此，项目已经进入“生产资料管理及任务—版本—自动单版本审核前后端主链接入，审核媒体可使用缩略图/网页代理并安全降级，四个既有隔离子集门禁已关闭”的状态。审核页面和媒体派生目前只有定向静态检查与单测，没有 FFmpeg/浏览器旅程；已完成的既有子集验证也不包含真实 UNC/SMB 目录创建，不是完整系统 E2E。仍不能跳过真实 NAS 部署门禁、权限撤权负向、Range 播放和文件页面直接宣称完整生产闭环。

本次新增的 `[x]` 只表示该条明确描述的代码或验证证据已完成。项目、镜头、资产及任务工作台/版本上传四个子集均已有 PostgreSQL/Redis/平台账号/生产代理浏览器证据；任务/版本子集的本地 TEMP 发布不是 UNC/NAS 证据。真实版本缩略图文件、Range 真分段、真实 UNC 门禁和完整系统 E2E 均未完成，不能把子集勾选解读为整个阶段或生产闭环已经验收通过。

## 3. 完成定义

项目完成必须同时满足以下条件。

### 3.1 产品范围

- 六个一级页面可用：工作台、项目、镜头管理、资产库管理、版本审核、文件与 NAS。
- 项目内部页面可用：概况、集与场次、镜头、资产、审核、成员。
- 任务详情、镜头详情、资产详情和版本审核形成连贯业务路径。
- 《需求.md》列出的 14 项 MVP 能力全部有真实实现和验收证据。
- “第一版暂时不做”的能力没有被偷偷并入 MVP。

### 3.2 数据闭环

```text
项目与成员
→ 集、场次、镜头和三类资产
→ 分配制作人并创建唯一视频或图片任务
→ 线下制作后上传文件
→ 自动创建不可覆盖版本、规范业务文件名和单版本审核单
→ 对指定版本批注
→ 退回或通过
→ 退回后生成下一版本，或确认最终版本并完成任务
→ 可选加入人工批量审核单
→ 刷新后仍可恢复完整状态
```

### 3.3 工程质量

- 前端技术栈和工程组织参考 `ruoyi-fastapi-frontend`。
- 后端遵循现有 Controller → Service → DAO → DO/VO 分层。
- PostgreSQL 结构变更同时通过 SQLAlchemy DO、Alembic 迁移和 `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql` 交付。
- 前端和后端都存在可重复执行的检查及测试命令。
- 核心链路存在真实集成测试和端到端测试。
- 生产配置不包含示例密码、私钥、Token 或默认密钥。

### 3.4 交付边界

以下结果不能单独作为项目完成证据：

- 静态原型或设计稿；
- Mock 数据可以操作；
- Swagger 中存在接口；
- 数据表已经建立；
- 前端构建成功；
- 服务健康检查成功；
- 只完成单个角色或单条成功路径；
- 只在不刷新页面时看起来正常。

## 4. 固定技术与产品决策

在用户没有明确变更前，以下决策作为计划基线：

- 业务前端是独立应用，不取代 `ruoyi-fastapi-frontend` 管理后台。
- 前端使用 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus、Sass 和 npm。
- 依赖版本、构建配置、认证和请求基础设施优先参考 `ruoyi-fastapi-frontend`。
- 后端复用现有 RuoYi FastAPI 服务，不另建第二套认证服务。
- 用户复用 `sys_user`，项目内身份通过项目成员关系表达。
- Shot Grid 新业务默认仅支持 PostgreSQL。
- 业务表使用 `sg_` 前缀。
- 业务 API 使用 `/shot-grid` 前缀。
- 权限码使用 `shotgrid:<resource>:<action>` 格式。
- 文件使用平台受保护文件和 `fileId` 引用机制。
- MVP 不交付客户独立门户；外部审核只保留未来兼容设计。

如果正式实现采用不同的 API 前缀、权限码或表名，必须先形成明确决策，并同步更新 `AGENTS.md`、本计划和接口文档。

## 5. 总体工作流

```text
阶段 0：契约与架构冻结
    ↓
阶段 1：应用基座
    ↓
阶段 2：PostgreSQL 与后端领域基础
    ↓
阶段 3：项目、集、场次、镜头、资产
    ↓
阶段 4：唯一制作任务与分配
    ↓
阶段 5：版本、文件与媒体
    ↓
阶段 6：批注与审核状态机
    ↓
阶段 7：审核单、工作台、搜索与文件页
    ↓
阶段 8：联调、质量门禁与发布
```

阶段之间允许在契约稳定后并行开发前端和后端，但不得让前端临时字段成为后端数据模型的事实标准。

### 5.1 当前执行与验证策略

当前处于 MVP 缺失功能快速补齐阶段，执行重点是尽快形成完整可用产品，验证采用“日常最小定向、阶段集中门禁、最终严格验收”的分级策略。

- 开发顺序优先补齐缺失页面、真实 API 接入和不完整业务闭环，不因每个小改动重复执行发布级检查而中断连续开发。
- 单个文案、样式、页面组件或局部逻辑完成后，只做与改动直接相关的静态检查、单个测试文件或一条关键界面交互；同一代码状态下已通过的检查不重复运行。
- 多个相关功能应作为一个迭代批次推进。批次结束或阶段准备关闭时，再集中运行全量 lint、完整测试、生产构建和必要浏览器旅程。
- 阶段工作项的 `[x]` 可以表示“实现完成并通过最小定向检查”；只有阶段出口门禁通过后，才表示该阶段整体完成。不得把定向检查表述为完整 E2E 或发布就绪。
- 认证、权限、数据隔离、事务、迁移、文件安全、并发和不可恢复操作仍需保留与风险直接相关的定向验证，但不扩大到无关模块。
- 阶段 8 负责跨模块联调、完整质量门禁和发布验收；产品功能全部补齐后，再统一执行严格的全链路 E2E、安全、性能、真实 NAS/UNC 和部署验收。
- 检查输出只保留命令结论、通过/失败数量和关键错误，不在日常迭代中保存或回传大段重复日志与构建产物清单。

## 6. 阶段 0：契约与架构冻结

### 目标

在创建大量页面之前，解决会影响所有模块的数据、权限、路由和媒体问题。

### 工作项

- [ ] 阅读并确认 `docs/参考项目评估.md` 的“采用、重做、放弃”清单，所有参考能力仍以当前契约重新设计。
- [x] 确认正式产品采用独立 `shot-grid-frontend`，系统管理保留在 `ruoyi-fastapi-frontend`。
- [x] 确认前端脚手架复用范围：对齐依赖版本，适配认证、统一请求、传输加密和基础协议，不运行时深层导入管理端源码。
- [x] 明确业务应用使用自有登录页，并复用平台 `Admin-Token`（`path=/`）；同域部署时可共享 Cookie 登录态，仍由业务端自行初始化用户和范围导航。
- [x] 确认业务路由使用本地白名单组件映射，并由 `/shot-grid/navigation` 返回应用范围菜单。
- [x] 定义项目角色：项目总监、制作人员，以及管理员的全局能力边界。
- [x] 冻结项目、集、场次、镜头、资产、任务、版本、审核意见和审核单的第一版字段字典；媒体探测扩展字段随媒体方案另行补充。
- [x] 冻结项目文件缩写、制作人缩写和两类产出物文件命名规则。
- [x] 冻结任务、版本、镜头、资产和审核主体状态代码及允许流转；“重新打开已完成任务”仍是后续产品决策。
- [ ] 确认图片和视频的格式、大小上限、存储位置和保留策略。
- [ ] 确认缩略图、视频代理和媒体元数据由哪个后台任务处理。
- [x] 冻结批注协议：毫秒时间点、归一化坐标、画笔/箭头/文字数据结构。
- [x] 冻结统一分页、排序、筛选、稳定错误键、真实 HTTP 状态和并发冲突响应；后续资源沿用同一约定。
- [x] 冻结当前镜头 Excel 样表结构：多 Sheet 表示多集、A:P 主表、“序→000”、镜头号集内唯一和焦段文本。
- [x] 为后端下载模板冻结匿名副本及 SHA-256 `F6370BBB14548B645782ABF0734E930EC10470565821BA6C8FD1B6A2D9D96EE0`；只改 workbook、sharedStrings 和 3 个 docProps XML，删除绝对路径、匿名化 88 条共享字符串与作者/应用属性并清空自定义属性，其余 13 个条目（含两个 Sheet、styles、theme）字节不变，解析仍为 total 24、valid 24、warning 0、error 0、2 集、8 场、24 镜头。
- [x] 冻结资产 Excel 当前正式样表、两类模板版本、最大行数、文件大小、名称规范化和原文件仅临时解析不长期保留的边界。
- [x] 冻结镜头先导、资产后导的 `sg_import_batch` 与 `sg_shot_asset_requirement` 数据关系、状态和 API 动作。
- [ ] 明确 NAS 路径只提供查看/复制，还是已有桌面协议处理器。
- [x] 建立正式接口清单和第一批最小 OpenAPI 契约；未实现章节保持设计状态。
- [ ] 建立关键决策记录，记录已选方案、未选方案和原因。

### 阶段出口

- 前后端字段名称、状态代码和主键类型一致。
- 文件上传、媒体播放、批注和审核动作有明确请求与响应契约。
- 不存在需要页面组件自行猜测的数据关系。
- 未决策项都有负责人或明确阻塞说明。

## 7. 阶段 1：应用基座

### 目标

建立可独立运行、可登录、可测试的业务前端骨架。

### 前端工作项

- [x] 参考 `ruoyi-fastapi-frontend` 初始化 Vue 3 + Vite 工程。
- [x] 建立并提交 `package.json` 和 `package-lock.json`。
- [x] 配置开发、测试、生产环境变量和开发代理，并提供固定 `/shot-grid-app/` 与剥离 `/prod-api/` 的 Docker/Nginx 配置；隔离生产镜像已验证页面根路径、详情深链和 API 前缀剥离。
- [x] 适配统一 Axios、Token、401、错误码、传输加密和二进制响应基础逻辑。
- [x] 适配用户信息、退出登录和 `/shot-grid/navigation` 权限加载。
- [x] 建立业务端独立 Layout。
- [x] 建立六个一级导航，未知键、重复键和路径不匹配项会被拒绝；项目页和镜头页已接入真实接口，其他四项仍保留清晰的无 Mock 接入边界。
- [x] 建立登录重定向、403、404 和 5xx 服务异常页面；401 由统一请求层清会话并返回登录页。
- [ ] 建立全局加载、空状态、失败状态和消息反馈规范。
- [x] 建立第一批基础主题、错误状态和响应式业务布局令牌；领域状态色随真实业务页补充。
- [x] 添加 `lint`、`test`、`build:prod` 脚本。
- [x] 建立第一批路由注册表、路由守卫、请求层、会话状态和传输加密策略测试。

### 后端配合

- [x] `/login` 与 `/getInfo` 已按业务端接入；`/getInfo` 使用不含 `password` 的专用安全用户 VO，前端也只投影必要身份字段。
- [x] 实现 `/shot-grid/navigation`，只返回 `route_name='ShotGrid'` 根节点下当前用户有权访问的白名单路由键。
- [ ] 配置业务端需要的 CORS、代理和传输加密路径。

### 阶段出口

- 使用真实后端账号完成登录、获取用户信息和退出。
- 未登录访问业务路由会被正确拦截。
- 无权限页面显示 403，不伪装成 404 或空数据。
- 刷新任意可访问路由后会恢复登录与路由状态。
- `npm.cmd run lint`、`npm.cmd run test` 和 `npm.cmd run build:prod` 可重复执行，并在阶段出口集中运行；不要求每个工作项完成后重复执行三项全量门禁。

当前验证记录：本批 `npm.cmd run lint`、独立全量 Vitest 32 个测试文件/148 个测试、`npm.cmd run build:prod` 均通过，生产构建处理 1796 个模块，仅保留既有 `@vueuse/core` PURE annotation 两条警告；后端 Ruff check 通过，Ruff format `--check` 报告 161 files already formatted，完整 `tests/module_shot_grid` 为 499 passed、2 skipped，两个跳过项均因当前 Windows 环境不允许创建目录符号链接，preflight 相关 3 个定向文件为 43 passed。项目、镜头、资产及任务工作台/版本上传四个子集浏览器证据均已完成。任务/版本子集使用 fresh PostgreSQL head 06/22 表、Redis DB 15 和真实平台登录，验证 21 条任务分页 20+1、keyword=1、task 900001 开始、5663-byte 图片三步提交、current 恢复、本地 TEMP `published → committed` attempt 1、V001/审核单/正式引用及同 SHA 下载；console/storage/logout/守卫和精确清理均闭合。阶段 1 仍不能作为整个应用完整验收通过；权限撤权负向、Range 206/416、MP4/MOV、真实 UNC/NAS、审核前端和完整系统 E2E 均未验证。

## 8. 阶段 2：PostgreSQL 与后端领域基础

### 目标

建立可靠的数据模型、权限基础和业务 API 骨架。

### 数据库工作项

- [x] 创建 `sg_project`。
- [x] 创建 `sg_project_member` 并关联 `sys_user`。
- [x] 创建 `sg_episode`。
- [x] 创建 `sg_scene`。
- [x] 创建 `sg_shot`。
- [x] 创建 `sg_asset`。
- [x] 创建 `sg_asset_item`。
- [x] 创建 `sg_shot_asset`。
- [x] 创建 `sg_task`。
- [x] 创建 `sg_version`。
- [x] 创建 `sg_version_file` 并关联 `sys_file_info`。
- [x] 版本暂存事务建立 `shotgrid_version_submission` 临时引用，正式版本事务切换为 `shotgrid_version` 主文件引用。
- [x] 创建 `sg_note`。
- [x] 创建不可覆盖回复历史 `sg_note_reply`。
- [x] 创建 `sg_review_action`。
- [x] 创建 `sg_review_list`。
- [x] 创建 `sg_review_list_version`。
- [x] 创建 NAS 根目录白名单 `sg_storage_root` 和项目存储绑定 `sg_project_storage`。
- [x] 创建目录 Outbox/租约表 `sg_storage_operation`。
- [x] 创建版本暂存与 NAS 发布编排表 `sg_version_submission`。
- [x] 创建镜头与资产导入批次表 `sg_import_batch`。
- [x] 创建镜头先导入后的资产需求表 `sg_shot_asset_requirement`。
- [x] 为集号、场次号、镜头号、单目标唯一任务、版本序号、自动审核单和审核单顺序添加数据库约束。
- [x] 为资产规范化名称、导入幂等键和镜头资产需求匹配键添加数据库约束。
- [x] 为资产制作分项建立可空导入字段和非空名称部分唯一索引。
- [x] 已有正式版本的资产制作分项禁止普通编辑主数据。
- [x] 在资产图片版本提交 Service 中校验制作分项名称必填且安全规范化后非空。
- [x] 为项目、状态、负责人、截止日期、任务和审核字段建立必要索引。
- [x] 提供 PostgreSQL Alembic 升级和降级迁移。
- [x] 同步更新 `ruoyi-fastapi-backend/sql/ruoyi-fastapi-pg.sql` 初始化基线。
- [x] 通过 `20260810_05` 增加目录操作执行状态一致性约束，以及项目聚合最新操作和项目创建时间分页索引。
- [x] 通过 `20260811_06` 增加“我的任务”索引、源文件唯一索引、包含 `failed` 的每任务未解决提交约束、提交执行/错误状态约束和审核动作持久化幂等字段；迁移 DDL 前执行冲突 guard。

### 后端工作项

- [x] 建立独立 Shot Grid 业务模块及 Controller、Service、DAO、DO、VO 分层骨架。
- [x] 建立 `/shot-grid` 路由前缀。
- [x] 建立 `shotgrid:<resource>:<action>` 权限码。
- [x] 建立 Shot Grid 六项菜单、按钮权限和项目/画幅/资产类型等字典种子。
- [x] 实现项目成员和项目角色权限依赖。
- [x] 项目、成员、集、场次、镜头、资产、制作分项、导入、任务、版本、自动审核和目录操作 Service/DAO 已补齐资源归属校验。
- [x] 任务动作接口已补齐平台权限、项目角色和任务负责人三层授权，并保留项目成员只读协作能力。
- [x] 建立统一分页、排序方向和乐观锁请求基础模型。
- [x] 已实现的项目、集、场次、镜头、资产、任务、版本、自动审核和目录操作查询 VO 已建立筛选与排序白名单。
- [x] 为可普通编辑的主数据 DO 统一审计字段、乐观锁和逻辑删除字段；历史、关系和编排表按契约使用专用字段。
- [x] 项目、成员、集/场次/镜头/资产普通管理和两类 Excel 正式提交已写入同事务操作日志。
- [x] 项目目录和动态目录人工重试使用新建 `reconcile_directory` 的不可变操作历史，并与原因及操作日志同事务提交。
- [x] 实现 PostgreSQL `FOR UPDATE SKIP LOCKED` 领取、owner + attempt fencing、租约心跳、退避重试、路径快照复核、幂等建目录和写权限探针。
- [x] 使用只由 Application Leader 注册的内部任务 `_shot_grid_storage_outbox` 驱动目录 Worker；非 PostgreSQL 或默认关闭时不注册。
- [x] 提供项目目录操作分页/详情、项目初始化人工重试和动态业务目录人工重试 API；分页关键字只检索安全诊断字段，默认按创建时间倒序，响应裁剪租约、幂等键、凭据及内部绝对路径。
- [x] 提供项目创建页专用的 `GET /shot-grid/storage-roots/options`、无副作用 `POST /shot-grid/storage-roots/{storageRootId}/project-path-preview` 和 `GET /shot-grid/member-candidates`；只返回健康根目录或有效平台用户的安全投影，创建事务仍重新校验全部关键条件。
- [x] 提供项目镜头页专用的 `GET /shot-grid/projects/{projectId}/shot-assignee-options`，只分页返回活动成员、有效平台用户且具有非空制作人缩写的安全投影；写事务仍重新校验。
- [x] `completed/archived` 项目下的集、场次、镜头创建/修改/归档与镜头导入 preview/commit 统一返回 HTTP 409 / `SG_INVALID_STATE_TRANSITION`，项目与镜头 `allowedActions` 同步收敛；只读列表与详情不在该写门禁内，资产、成员、任务、版本、审核、文件和目录操作等全域终态治理仍是后续工作。
- [x] 项目成员写操作在锁定项目后重新解析操作者访问范围并要求其仍为项目总监，关闭等待行锁期间被移除/降级的竞态窗口。
- [x] 任务分配/改派/开始、版本暂存/正式提交和审核意见/回复/解决/动作已接入同事务操作日志；后续 `manual_batch` 和资产需求人工处理仍需沿用。
- [ ] 实现基础健康或诊断信息，但不以此替代业务测试。

### 测试工作项

- [ ] 建立完整 RuoYi 平台 Alembic baseline，使 Alembic 可从真正空库独立创建全部平台表；当前 Shot Grid revision 是已有平台库上的增量迁移。
- [x] 更新后的 PostgreSQL 初始化 SQL 可从新库建立当前全量结构并写入 Alembic head。
- [x] 迁移在已有基线数据库上可执行升级、降级和再次升级。
- [x] 初始化 SQL 建库结果与 Alembic 最新版本的 Shot Grid 表、约束和索引一致。
- [x] 数据库约束拒绝无归属任务、同目标重复任务、重复版本号、重复自动审核单、重复审核单顺序和非法主文件角色。
- [x] 项目访问 Service 的针对性测试证明非项目成员被稳定拒绝；完整浏览器越权 E2E 仍留在阶段 8。
- [x] 项目创建及两类 Excel 提交 Service 已覆盖针对性事务回滚测试；跨服务完整 E2E 仍留在阶段 8。
- [x] 本批后端 Ruff check 通过，Ruff format `--check` 报告 161 files already formatted；完整 `tests/module_shot_grid` 为 499 passed、2 skipped，两个跳过项均因当前 Windows 环境不允许创建目录符号链接；preflight 相关 3 个定向测试文件为 43 passed。
- [x] 项目选项/成员候选、镜头制作人选项和镜头模板路由已在 OpenAPI/路由注册测试中确认；两类成员候选查询、项目创建和添加/恢复成员均应用 `DataScope(SysUser)`，成员写入在项目行锁后重新校验操作者仍为 `director`。
- [ ] 使用正式 Windows Worker 服务账号和隔离 UNC 根目录验证项目初始化、动态目录、租约接管、权限失败和人工重试；当前临时本地目录测试不等于真实 NAS E2E。

### 阶段出口

- 数据模型通过迁移建立，不依赖 `create_all()`。
- OpenAPI 中能够识别 Shot Grid 接口和鉴权要求。
- 项目级数据权限在后端生效。
- 关键数据库不变量由约束或服务层测试证明。

## 9. 阶段 3：项目、集、场次、镜头与资产

### 目标

完成项目生产资料的基础录入和组织能力。

### 工作项

- [x] 项目创建事务、成员范围列表、详情和真实概览聚合。
- [x] 项目编辑和归档；状态字段不能通过普通 PUT 绕过独立动作。
- [x] 项目成员添加、软移除、恢复和项目角色调整。
- [x] 独立业务前端项目页接入真实范围列表、筛选/排序/分页、创建及 HTTP 202 初始化中反馈、详情/概览、完整字段编辑、独立归档、成员维护和存储操作诊断；不回退 Mock。
- [x] 创建项目页面从后端健康根目录选项和安全成员候选中选择，并使用后端路径预览；前端不提交 UNC 根路径，也不把逻辑 `healthy` 记录描述为真实 NAS 验收。
- [x] 使用隔离真实 PostgreSQL、Redis、平台管理员账号和生产 Nginx 完成项目列表、创建 HTTP 202、详情/概览、深链刷新、编辑、成员候选及增改移、存储操作详情、归档/列表回查与退出的项目管理子集浏览器旅程。
- [x] 项目代号直接作为唯一业务文件名前缀，制作人员保存项目内唯一制作人缩写；不再维护独立 `file_prefix`。
- [x] 集创建、编辑、排序和项目内查询。
- [x] 场次创建、编辑、排序和集内查询。
- [x] 镜头创建、编辑、列表、详情和业务归档。
- [x] 镜头业务界面按契约提供真实项目/集/场次/状态/制作人筛选、制作字段、资产关联、任务、最新版本/反馈和只读聚合数据；界面列与 Excel 导入列保持明确边界。
- [x] 镜头表服务端分页、搜索、筛选和排序。
- [x] 镜头表格、卡片和故事板共享同一服务端查询结果；项目切换与筛选会取消旧请求，不维护三套漂移数据。
- [x] 镜头详情、创建/编辑、首次分配/改派和归档接入真实接口，并按后端 `allowedActions`、平台权限、项目角色、项目状态与存储状态控制入口。
- [x] 后端缩略图相对 URL 经统一鉴权请求层获取 Blob，403/404 安全占位，切换/卸载时中止并释放 Object URL；该项不等于已实现缩略图生成或媒体代理。
- [x] 资产及制作分项创建、编辑、列表、详情和业务归档。
- [x] 资产 Excel 导入和普通管理 API 均只接受 `Character`、`Environment`、`Prop`。
- [x] 资产表按资产父级与制作分项层级提供类型、名称、描述、唯一任务、状态和单一主制作人聚合数据。
- [x] 资产真实页面完成项目/类型/状态/制作人/关键字筛选、服务端分页、表格/卡片/类型看板、详情深链、资产与分项 CRUD、首次分配/改派、后端 `allowedActions` 和受保护缩略图空态；最终 lint/test/build 已通过，隔离浏览器旅程实际覆盖三视图、Environment/蒋浩筛选、详情深链、CRUD/归档和改派。真实版本缩略图文件未构造，不属于本项完成证据。
- [x] 资产/制作分项响应由后端计算 `asset.edit`、`assetItem.add`、`asset.archive`、`assetItem.edit`、`assetItem.archive`、`task.assign`；分项只取当前最新版本缩略图，父资产按活动分项稳定顺序选择代表图，不回退旧版本。
- [x] 两类导入已支持幂等创建和唯一匹配 `sg_shot_asset`；镜头普通编辑已支持完整 `assetIds` 关系同步，资产需求冲突可人工选择同类型正式资产、填写原因解决或忽略，并可项目级重新执行唯一匹配。
- [x] 按真实样表实现多 Sheet 镜头导入：Sheet 名派生集，A:P 固定表头，忽略只读列与空白分隔后的辅助区。
- [x] 镜头导入支持“序→000”、集内镜头号唯一、焦段文本无损保留，并按去重键统计和创建集、场次、镜头。
- [x] 镜头样表中的场景资产未建立时只形成 `Environment` 待匹配需求，不从文本猜测角色或道具。
- [x] 按资产正式样表实现三类资产统计、预检查、错误报告和事务化正式导入；原样表为 12 个父资产/20 个制作分项，preview 为 total 20、valid 19、warningRows 3、errorRows 1。
- [x] 实现鉴权镜头 Excel 模板下载端点及前端 Blob 下载；打包模板摘要、24 行/2 集/8 场/24 镜头统计和无本机/网络路径泄露由测试冻结。
- [x] 实现资产 Excel 模板鉴权下载端点及前端 Blob 下载；已用 `artifact_tool` 参考原样表重建匿名 `asset-v1`，完成渲染复核、公式错误扫描、冻结 SHA-256 和服务端摘要失败关闭，未打包或透传含真实人名/项目内容的原样表。
- [x] 镜头导入弹窗实现 `.xlsx` 上传、preview 问题展示、按 Sheet/行选择、组件内短期 Token 与稳定幂等键、commit 和结果回显。
- [x] 资产导入弹窗完成 20/19/3/1 汇总、逐行问题、可提交行选择、内存 Token、稳定幂等键、commit 与自动匹配/待处理/冲突结果回显；隔离旅程一次提交 19 个可导入行，生成 11 个活动资产、19 个分项/任务和 1 个显式夹具自动匹配。模板下载不属于本项成功条件，且仍未交付。
- [x] 资产导入后自动匹配唯一镜头需求，保留待处理和冲突状态。
- [x] 提供资产需求分页、同类型正式资产人工解决、有原因忽略和项目级重新匹配 API，并在资产页用 Element Plus 表格/筛选/选择组件接入；重新匹配只处理 `pending/conflict`，不覆盖 `ignored`。
- [x] 项目概况统计使用真实聚合数据。
- [x] 已受理的项目、集、镜头和资产目录 Outbox 已有 Worker 消费路径；生产启用仍受真实 UNC/ACL 验收门禁约束。
- [x] 已使用隔离真实 PostgreSQL、Redis DB 15、FastAPI/平台账号、生产 Nginx 和 Chrome 完成镜头三视图、详情、任务展示、模板、preview/commit、筛选、深链刷新和退出保护旅程；模板 11883 bytes 且摘要正确，preview 24/24、0 警告行、0 错误行，commit HTTP 200 并创建 2 集、8 场、24 镜头、24 任务、24 待匹配需求与 26 条待处理目录操作。该项不包含真实 UNC/NAS。
- [x] 已使用隔离真实 PostgreSQL、Redis DB 15、FastAPI/平台账号、最终生产构建、生产 Nginx 和 Chrome 完成资产三视图、筛选、详情深链/刷新、资产/分项 CRUD 与归档、任务改派、缩略图空态、20/19/3/1 preview、19 行 commit、控制台 0 error/0 warning、浏览器存储、退出保护、数据库/Redis/审计和清理旅程；7 条审计全成功，12 条 Outbox 因 Worker 关闭保持 `pending`，环境零残留。模板下载、真实版本缩略图文件和真实 UNC/NAS 明确排除。

### 阶段出口

- 项目总监可以创建项目、添加成员，按顺序导入镜头表和资产表。
- 导入错误能定位到行和字段，不产生半成功脏数据。
- 镜头导入正确创建去重的集、场次和镜头；资产不存在时形成待匹配需求，不产生假资产。
- 资产导入能按三种类型统计新增数量，唯一匹配自动关联镜头，冲突留在人工处理列表。
- 表格、卡片和故事板切换后筛选条件与数据一致。
- 制作人员只能访问其所属项目。
- 刷新和重新登录后数据保持正确。

## 10. 阶段 4：唯一制作任务与分配

### 目标

完成镜头视频任务和资产图片任务的分配与状态链。

### 工作项

- [x] 镜头 Excel 导入及镜头普通创建/编辑在尚无任务时支持首次分配，并创建唯一 `shot_video` 任务。
- [x] 资产 Excel 导入及资产/制作分项普通创建/编辑支持首次分配，每个制作分项最多一个 `asset_image` 任务；复合制作人值拒绝导入。
- [x] 资产导入中制作分项名称缺失只警告并允许落库；资产图片版本提交前强制补齐。
- [x] 后续改派更新同一任务并记录审计，不重复创建正式任务；任何非 `committed` 提交（包括 `failed`）都会阻止改派。
- [x] 实现独立任务编辑与分配动作，支持负责人改派、优先级、截止日期和制作要求修改，并使用 `taskLockVersion/lockVersion` 防并发覆盖。
- [x] 建立任务详情，聚合所属对象、项目、负责人、版本摘要和允许动作；完整版本与意见继续使用独立分页接口。
- [x] 后端契约明确平台不执行图片或视频制作，只承接线下产出物上传和审核。
- [x] 建立跨项目 `GET /shot-grid/tasks/mine`，支持按任务类型、状态、截止日期、优先级、关键字和排序筛选；真实工作台已接入服务端分页并可进入 `/tasks/:taskId`。
- [x] 建立任务 `not_started → in_progress → pending_review → revision/completed` 状态动作和权限校验。

### 阶段出口

- 未分配目标没有空负责人任务，首次分配后只生成一个正确类型的任务。
- 任务只能属于一个镜头或一个资产制作分项。
- 制作人员只能修改被授权任务，项目总监可以分配任务。

## 11. 阶段 5：版本、文件与媒体

### 目标

完成不可覆盖的版本历史和可审核媒体链路。

### 文件工作项

- [x] 复用平台私有文件上传和 `fileId` 引用；暂存时创建临时引用，正式提交时切换主文件引用。
- [x] 定义主审核文件和可选辅助文件的用途字段；当前任务提交只强制一个符合类型约束的主产出物。
- [x] 版本已关联主审核文件；缩略图、代理文件和源文件属于经确认后再实现的辅助能力，不得阻塞主上传审核闭环。
- [x] 镜头任务只允许 MP4/MOV，资产任务只允许 JPG/PNG，并按真实字节校验 JPEG/PNG 与 MP4/MOV 容器签名/品牌；尚未探测 codec、视频轨或执行转码。平台上传当前上限为 100 MiB，`mov` 已加入白名单。
- [x] 平台物理文件名保持不变，规范业务文件名保存到版本文件关系。
- [x] Shot Grid 专用授权下载按净化后的业务文件名设置下载名称，保留 Range，显式 deny ACL 优先。
- [x] 文件按 attempt 唯一临时名发布到 NAS，校验真实大小和 SHA-256 后无覆盖原子发布；数据库事务内不执行 NAS I/O。
- [x] NAS 发布完成后，在短事务中保持文件引用、正式版本、业务文件名、`auto_single` 审核单、任务状态和提交状态一致。
- [x] 文件仍被临时提交或正式版本引用时由平台引用机制阻止直接永久删除。
- [ ] NAS 路径经过权限校验并安全展示、复制。

### 版本工作项

- [x] 后端锁定项目和任务并通过 `sg_version_submission` 保留 V001、V002 等序号；重试复用原序号、时间戳和业务文件名。
- [x] 镜头按“项目缩写、EP 集、场次、S 镜头、制作人缩写、版本号、服务端时间戳”生成业务文件名；资产按类型、名称、制作分项、制作人、版本号和时间戳命名。
- [x] 镜头名称严格满足 `{项目缩写}_EP{集号至少3位}_{场次号至少3位}_S{镜头号至少3位}_{制作人缩写}_V{版本号至少3位}_{服务端毫秒时间戳}.{mp4|mov}`。
- [x] 资产名称严格满足 `{项目缩写}_Asset_{Character|Environment|Prop}_{资产名称}_{制作分项}_{制作人缩写}_V{版本号至少3位}_{服务端毫秒时间戳}.{jpg|png}`。
- [x] 每次提交创建新版本，禁止覆盖旧版本；同一源文件只能属于一个提交。
- [x] 记录提交人、提交时间和修改说明。
- [x] 每次版本提交自动创建一个只包含当前版本的 `auto_single` 审核单。
- [x] 版本提交与详情支持保存和读取图片/视频 AI 生成参数快照；细粒度参数权限裁剪仍需后续补齐。
- [ ] AI 参数默认折叠并受权限控制。
- [x] 在私有上传前增加只读、无锁、无写库的 `POST /shot-grid/tasks/{taskId}/version-submissions/preflight`；请求体为 `fileName/fileSize/changelog/aiParams`，预检上下文、权限、活动提交、扩展名、业务文件名、目标相对路径可生成和目录快照字段完整。它不访问 NAS 或检查实际目标文件；真实创建仍在上传后锁内全量复检，实际目标文件冲突由 Worker 无覆盖发布处理。
- [x] 前端固定执行“本地校验 → private preflight → `/common/files/upload` 私有上传 → 携带内存幂等键 create 202”；预检失败、权限撤销、任务/文件 ABA 时零上传，create 结果未知时复用原 `fileId` 和幂等键重放并跳过预检/上传。
- [x] 提供 `current` 恢复、状态查询和 `failed` 原行重试；自动轮询单轮最多 30 次、连续错误 3 次暂停、指数退避有上限，401/403/404 停止，只有 `committed` 才显示成功。
- [x] 提供任务版本分页与版本详情 API；前端已接入历史、详情和受保护 Range 下载，`/versions/:versionId` 使用 `reviews` 路由范围。AI 参数细粒度展示和审核交互仍待实现。

### 媒体工作项

- [ ] 提取图片和视频基础元数据。
- [ ] 生成缩略图。
- [ ] 根据冻结方案生成网页审核代理文件。
- [ ] 媒体处理具有排队、处理中、成功、失败状态。
- [x] 图片预览继续通过统一鉴权接口获取 Blob；视频先经鉴权接口领取资源和登录会话绑定的 Redis 短期票据，再由原生 `<video>` 对票据 URL 发起 Range 请求。每次分段请求重查账号权限、项目成员关系、文件关系和显式 deny ACL。
- [x] 审核媒体鉴权、空文件、类型不支持和加载失败显示明确状态，不使用永久占位假装成功。
- [x] 审核媒体在版本切换或页面卸载时中止旧请求并释放 Blob URL。

### 阶段出口

- 上传文件、NAS 发布和正式版本短事务全部完成后才真实形成 V001、规范业务文件名和自动审核单；失败不得显示为版本成功，再次提交形成 V002 及新审核单。
- V001 的文件、参数和审核记录不会被 V002 覆盖。
- 图片可以预览，视频可以拖动并通过 Range 请求播放。
- 无权用户不能直接通过 URL 下载媒体。
- 超限文件、格式错误、处理失败和 Range 错误有明确反馈。
- 刷新后仍能查看版本历史与媒体处理状态。

## 12. 阶段 6：批注与审核状态机

### 目标

完成版本级审核、反馈和最终版本控制。

### 2026-08-14 正式基线重构

- [x] 提供 PostgreSQL Alembic 迁移和同步初始化 SQL：为问题增加解决版本，新增版本逐条处理说明与逐版本确认记录；按新基线直接删除旧回复和旧意见数据。
- [x] 重构版本 preflight/create：`issueResponses` 精确覆盖当前全部 open 问题，锁内重检问题集合并纳入幂等命令；首版不携带问题响应。
- [x] 提供任务 open 问题、版本 review-context、当前版本新问题和审核动作逐条确认 API；移除 reply/resolve 入口。
- [x] 重构制作人 `/tasks/:taskId`：完整展示来源版本文字/标注/时间点，并在提交新版本前逐条填写“本版如何处理”。
- [x] 重构审核页：先核验历史问题，再提出当前版本新问题；`approve` 要求全部关闭，`reject` 要求至少一条仍存在或新问题，`defer` 不伪造确认。
- [x] 移除聊天式回复、普通/必须修改开关和普通用户“标记已解决”；旧沟通数据不迁移。
- [ ] 增加 V001 退回 → V002 部分解决并新增问题 → V003 全部解决的后端定向测试、前端交互测试和一条关键浏览器旅程；另测遗漏处理说明、遗漏确认、跨任务问题、并发审核和幂等重放。

下面已勾选条目描述 2026-08-14 之前的旧审核模型实现，仅用于评估可复用媒体、批注、权限和并发基础；与上面的重构项发生冲突时，以新正式基线为准。

### 批注工作项

- [x] 后端支持版本绑定的文字审核意见。
- [x] 后端支持视频毫秒时间点，并校验不超过镜头时长；资产图片拒绝时间点。
- [x] 后端支持归一化结构化批注、安全可扩展类型和复杂度上限，可承载画圈批注。
- [x] 箭头批注使用归一化起止点，前端按媒体画幅换算线段长度和角度并随舞台缩放还原。
- [x] 文字批注通过 Element Plus 输入框录入，在归一化锚点渲染纯文本；后端继续执行 1000 字符、HTML/Data URL/Blob URL 拒绝规则。
- [x] 后端批注 item 支持安全文本；前端画面文字交互仍待实现。
- [ ] 参考图片附件。
- [x] 支持 `isMandatory`、open/resolved 状态和独立解决动作。
- [x] 支持有权制作人员或项目总监新增不可变回复及回复分页。
- [x] 点/矩形/箭头/文字批注先保存在当前意见表单草稿中，提交意见时才写入正式版本意见；清空、切换详情和提交成功均清理草稿。
- [x] 审核详情切换或刷新时以 AbortController 与 generation 拒绝迟到响应，意见只提交到当前详情绑定版本。

### 审核工作项

- [x] 后端确认通过直接形成最终版本、完成任务，并阻止 open mandatory 意见下通过。
- [x] 后端退回修改要求原因或 open mandatory 意见。
- [x] 后端稍后决定只记录不可变动作，不改变待审核状态。
- [x] 审核动作已实现平台接口权限、项目角色和资源归属校验。
- [x] 审核动作及意见/回复/解决写入同事务操作审计。
- [x] 通过数据库约束和锁序保持最终版本唯一性。
- [x] 退回后任务进入 `revision`，再次提交生成新版本和新 `auto_single` 审核单。
- [x] 后端并发审核使用版本 `lockVersion` 返回 409；前端显示冲突提示并刷新最新状态。
- [x] 审核事务保持版本、任务和自动审核单状态一致，并持久化动作幂等请求哈希和结果快照。

### 前端工作项

- [ ] 审核页左侧版本列表。
- [x] 中间图片播放器通过鉴权 Blob 加载；视频播放器已改为短期票据 URL，使浏览器可发起真实 Range 请求。后端 200/206/416 契约和前端票据接入已有定向代码证据，真实 MP4/MOV 浏览器网络旅程仍待集中验证。
- [x] 可选择同一任务的历史版本进行只读 A/B 对比，当前版本意见仍严格绑定 A 版本。
- [x] 右侧当前及历史意见，并支持不可变回复和独立解决动作。
- [x] 底部确认通过、退回修改、稍后决定三个审核动作，携带稳定幂等键和版本 `lockVersion`。
- [x] 视频可捕获当前毫秒时间并从意见跳转；点/矩形/箭头/文字坐标按媒体自然尺寸归一化，图层随等比例舞台缩放。真实图片/视频浏览器缩放旅程仍待完成。
- [x] 被退回版本提供任务版本工作区入口，修订继续创建新版本而不覆盖旧版本。

### 阶段出口

- 审核意见始终绑定正确版本。
- 视频时间点和画面标注在不同显示尺寸下保持位置正确。
- 退回后可以再次提交新版本。
- 两人并发设置最终版本时数据库仍只有一个最终结果。
- 制作人员不能执行项目总监审核动作。
- 无权用户的响应中不包含隐藏的提示词、费用或内部讨论。

## 13. 阶段 7：审核单、工作台、搜索与文件页

### 目标

完成跨对象组织、集中审核和六个一级页面。

### 审核单

- [x] 后端提供每次版本提交自动生成的 `auto_single` 单版本审核单列表和详情。
- [ ] 创建、编辑和归档人工批量审核单。
- [ ] 添加和移除版本。
- [ ] 拖动调整版本顺序并持久化。
- [ ] 连续播放或连续查看。
- [x] 从审核单进入单版本审核详情。
- [x] 自动审核单详情保留版本原始任务和对象归属，前端不复制或改写归属。

### 工作台

- [x] 后端提供跨项目“我的制作任务”分页，并可按任务状态、截止日期和优先级筛选。
- [x] 真实工作台调用 `/shot-grid/tasks/mine`，支持服务端筛选、排序、分页和空/错态，行项进入真实任务详情。
- [x] `/tasks/:taskId` 任务详情接入真实详情、开始与编辑；平台权限和后端 `allowedActions` 必须同时通过，切路由/重载使用 AbortController 与 generation 防迟到写入和 ABA。
- [x] 后端可通过 `taskStatus=revision` 查询我的待修改任务；前端可直接筛选修改中任务。
- [x] 工作台通过服务端跨项目权限查询展示“待我审核”，只返回当前用户担任项目总监的活动项目审核单；管理员按全局范围读取。
- [x] 今日截止可由截止日期区间筛选得到。
- [x] 修改中任务可由 `revision` 状态筛选得到。
- [x] 工作台通过服务端当前用户约束展示“最近提交”，不从当前任务分页在前端推断。
- [x] 工作台任务能够进入真实任务详情。

### 搜索与文件

- [x] 跨镜头、资产和正式版本文件的统一全局搜索；顶部入口与 Ctrl/Cmd+K 调用 `/shot-grid/search`，服务端按项目成员范围、全局范围权限和三类资源权限分别裁剪，前端使用 Element Plus 弹窗、250ms 防抖、AbortController 与 generation 丢弃过期响应。
- [x] 正式版本文件按项目、任务种类、任务和版本追溯；镜头或资产归属继续通过任务详情推导，不在文件关系重复保存。
- [x] 查看文件版本、任务和文件用途。
- [x] 通过 Shot Grid 受保护接口下载版本文件。
- [x] 查看和复制项目 UNC 安全快照及文件 NAS 相对路径；浏览器不伪装直接打开共享目录。
- [x] 文件中心只查询活动正式版本文件关系，禁止展示无版本业务归属文件或内部物理存储键。

### 阶段出口

- 审核单顺序刷新后保持不变。
- 工作台数据来自真实权限范围和状态。
- 搜索不会返回其他项目或无权资源。
- 六个一级页面和项目内部页面均不存在纯占位功能。

## 14. 阶段 8：联调、质量门禁与发布

### 功能联调

- [ ] 前端、后端、PostgreSQL 和 Redis 使用正式开发配置联调。
- [x] 独立业务前端生产 Dockerfile、Nginx SPA 深链回退及 `/prod-api/` 前缀剥离配置已实现，并在隔离真实 PostgreSQL、Redis、后端和平台账号环境完成项目管理子集浏览器旅程。
- [x] 已在同等隔离真实环境完成镜头列表三视图、详情、任务展示和镜头 Excel 导入浏览器旅程；数据库、Redis 预检键、控制台与退出保护均已核验，逻辑 ready/Worker 关闭边界另行保留。
- [ ] 文件上传、业务引用、下载、Range 播放和媒体处理联调。
- [ ] 权限码、项目成员权限和数据范围联调。
- [ ] 刷新、重新登录、项目切换和异常恢复联调。

### 自动化测试

- [x] 独立业务前端本批 lint、32 文件/148 单测和生产构建通过，构建处理 1796 个模块，仅保留既有 `@vueuse/core` PURE annotation 两条警告；除既有项目/镜头/资产竞态门禁外，还覆盖任务工作台/详情、三步版本提交、current 恢复、有界轮询与 failed 重试、历史/详情/下载、权限双门禁、内存幂等/ABA 和 Blob 错误保真。
- [x] Shot Grid 后端 Ruff check 通过，Ruff format `--check` 为 161 files already formatted，完整模块测试 499 passed、2 skipped，preflight 相关 3 文件 43 passed；当前 Windows 环境不能创建目录符号链接导致的跳过已明确记录，不冒充通过。
- [ ] 前端状态映射、批注坐标、时间格式和纯函数单元测试。
- [ ] 路由守卫、Pinia 和核心组件测试。
- [ ] 后端 Service、权限、事务和状态机测试。
- [ ] PostgreSQL 迁移测试。
- [ ] API 集成测试。
- [ ] 关键链路真实 E2E。

### 安全与隐私

- [x] 两类 Excel 已覆盖 ZIP/OOXML 资源放大、隐藏 Sheet、单格与总文本、预览 JSON 大小的针对性门禁测试；真实网关压测仍待阶段 8。
- [ ] 项目成员越权测试。
- [ ] 资源 ID 枚举和跨项目引用测试。
- [ ] 文件名、评论、提示词和项目描述 XSS 测试。
- [ ] 私有文件直接 URL 访问测试。
- [ ] Token 过期和权限变更后的缓存失效测试。
- [ ] 日志、响应和前端状态敏感信息检查。
- [ ] 生产 CORS、密钥、数据库和 Redis 配置检查。

### 性能与可靠性

- [ ] 在两个以上应用 Worker 场景验证 Application Leader 失锁、新 Leader 接管、过期目录租约重领及 owner + attempt fencing。
- [ ] 在隔离 UNC 根目录验证慢 SMB I/O 下的心跳续租和软超时诊断；软超时不得被误解为能硬杀文件系统线程。
- [ ] 当前目录 Worker 单轮串行消费；如后续启用批内并发，必须单独验证吞吐、同聚合互斥、数据库连接数和 NAS 限流。
- [ ] 镜头和资产长列表分页或虚拟化验证。
- [x] 全局搜索防抖和过期请求取消已由定向前端单测覆盖；真实跨项目数据量下的响应时间与 PostgreSQL 索引仍留待阶段 8 压测。
- [ ] 大媒体上传失败与重试验证。
- [ ] 真实 MP4/MOV 的浏览器拖动和 206/416 网络旅程验证；代码层 Range 流与短期播放票据已接通。
- [ ] 媒体对象和 Blob URL 内存释放验证。
- [ ] 并发版本提交和并发审核验证。
- [ ] 资产工作簿重新保存、移动行或更改 Sheet 后重导的治理验证，重点覆盖多个未命名制作分项不得静默复制。
- [ ] PostgreSQL 慢查询和必要索引检查。

### 发布资料

- [ ] 本地开发说明。
- [ ] 环境变量说明。
- [ ] PostgreSQL 迁移说明。
- [ ] 媒体处理依赖和部署说明。
- [ ] 备份、恢复和文件存储说明。
- [ ] 常见故障排查说明。
- [ ] 权限码和角色矩阵。
- [ ] API 契约或 OpenAPI 快照。
- [ ] 发布检查清单和回滚方案。

### 最终出口

- 所有 MVP 映射项完成。
- 自动化检查通过。
- 真实 E2E 通过。
- 不存在阻断级安全问题。
- 生产部署和回滚步骤可重复执行。
- 未完成项被明确标注为非 MVP 或阻塞项，没有用 Mock、手工改库或临时跳过掩盖。

## 15. MVP 需求映射

| 需求 | 主要阶段 | 完成证据 |
| --- | --- | --- |
| 创建项目 | 阶段 3 | 真实 PostgreSQL CRUD、项目权限测试 |
| 导入镜头表 | 阶段 3 | 模板、预检查、错误报告、事务导入测试 |
| 导入资产表并匹配镜头需求 | 阶段 3 | 类型统计、事务导入、唯一自动匹配、冲突人工处理和幂等测试 |
| 创建角色、场景、道具资产 | 阶段 3 | 资产 CRUD 和类型筛选 |
| 自动生成任务 | 阶段 4 | 首次分配生成单目标唯一任务测试 |
| 分配任务给成员 | 阶段 4 | 成员选择、权限和工作台验证 |
| 上传图片和视频版本 | 阶段 5 | 受保护上传、格式校验、两类规范业务文件名、文件引用、V001/V002、自动审核单 |
| 视频在线播放 | 阶段 5 | 鉴权 Range 播放和拖动验证 |
| 图片和视频批注 | 阶段 6 | 坐标、时间点和缩放恢复测试 |
| 审核通过与退回 | 阶段 6 | 状态机、权限、事务和审计 |
| 查看历史版本 | 阶段 5 | 不可覆盖版本和刷新恢复 |
| 创建审核单 | 阶段 5、7 | 自动单版本审核单、人工批量审核单、有序版本关系和连续审核 |
| 关联 NAS 文件路径 | 阶段 5、7 | 权限校验、查看和复制路径 |
| 搜索镜头、资产和文件 | 阶段 7 | 项目范围搜索和越权测试 |
| 根据权限显示内容 | 阶段 2—8 | 后端授权、响应裁剪和前端展示 |

## 16. 关键风险与控制措施

| 风险 | 影响 | 控制措施 |
| --- | --- | --- |
| 先做全量页面、后定接口 | 字段和状态反复返工 | 阶段 0 先冻结契约 |
| 直接复用全部 `/getRouters` | 业务端加载管理端菜单失败或越权 | 使用 `/shot-grid/navigation` 范围菜单和本地路由白名单 |
| 直接使用代码生成器的 CRUD 完成核心流程 | 绕过项目权限、状态机和跨表事务 | 生成器只产出骨架，核心 Service、状态机和测试人工实现 |
| 复用现有用户 Excel 直接落库模式 | 多表导入半成功且无法预览冲突 | 使用批次、preview/commit、Redis Token、全事务和幂等 |
| 镜头导入时隐式创建资产 | 拼写错误产生重复正式资产和错误任务 | 先建立待匹配需求，资产后导入只匹配唯一候选 |
| 重建用户表 | 两套账号和权限漂移 | 强制复用 `sys_user` |
| 文件只保存 URL | URL 失效且无法治理引用 | 保存 `fileId` 并复用业务引用 |
| 大视频直接上传后同步处理 | 请求超时、资源耗尽 | 冻结大小限制并采用异步媒体处理 |
| 前端生成版本号 | 并发出现重复版本 | 后端事务生成并添加唯一约束 |
| 标注保存像素坐标 | 播放器缩放后位置漂移 | 使用归一化坐标和自然尺寸 |
| 状态只在前端流转 | 可绕过权限和流程 | 后端状态机、事务和审计 |
| NAS 路径直接 `window.open` | 浏览器拦截或安全风险 | MVP 查看/复制，桌面协议另立项目 |
| 只因 Worker 源码或临时目录测试通过就启用生产 NAS | 服务账号权限、共享 ACL、UNC 语义和故障恢复未验证 | 默认关闭 Worker；使用正式 Windows 账号和隔离 UNC 根目录完成 E2E 后再显式启用 |
| 把软超时当作可中止 SMB I/O | 租约释放后旧线程仍可能继续写目录 | 超时只做诊断并继续心跳；等待物理 I/O 退出后再处理租约结果 |
| 外部审核提前进入 MVP | 匿名授权和数据隔离扩大范围 | 当前只交付内部审核 |
| PostgreSQL 迁移链与初始化 SQL 不一致 | 新环境和升级环境结构漂移 | 每个结构变更同时提交并核对 Alembic 与 PostgreSQL 初始化 SQL |
| 资产来源行键依赖文件摘要和物理坐标 | 工作簿重存或移动行后，未命名制作分项可能被当作新行 | 后续模板引入稳定 `rowUid`，或建立疑似重复提示与人工治理；当前不宣称跨文件逻辑幂等 |
| Mock 请求失败回退 | 掩盖真实后端不可用 | 生产代码禁止自动回退 Mock |

## 17. 阶段门禁

每个阶段准备标记为整体完成时进行一次集中门禁评审；该评审不下沉为每个工作项或每次代码修改后的全量检查：

1. 需求范围是否仍与 `需求.md` 一致。
2. 是否违反 `AGENTS.md` 的数据和权限不变量。
3. 前后端契约是否已同步。
4. PostgreSQL 迁移是否可重复执行。
5. 成功、失败、无权限和并发路径是否有验证。
6. 是否保留用户已有变更且没有提交敏感文件。
7. 实际验证结果是否与交付说明一致。

门禁未通过时，不得将该阶段标为完成。

## 18. 排期与负责人

本计划暂不填写虚假的日历工期。正式排期前需要确认：

- 前端、后端、测试和运维投入人数；
- 是否已有可复用的媒体转码服务；
- 文件大小和并发量级；
- 设计稿是否需要同步交付；
- 部署环境和对象存储/NAS 条件；
- MVP 是否需要真实外部用户参与验收。

确认后，为每个阶段补充：

| 阶段 | 负责人 | 计划开始 | 计划完成 | 当前状态 | 阻塞项 |
| --- | --- | --- | --- | --- | --- |
| 阶段 0 | 待定 | 待定 | 待定 | 进行中：主体契约与 Excel 参数已冻结 | 媒体与部署方案 |
| 阶段 1 | 待定 | 2026-08-11 | 待定 | lint、32 文件/148 测试和生产构建通过（1796 modules，仅既有 PURE annotation 两条警告）；项目、镜头、资产及任务/版本四个隔离子集旅程已通过；审核媒体相关 6 项、文件页 2 项定向前端单测通过，全应用门禁未关闭 | 审核/文件浏览器旅程、权限撤权负向及完整系统 E2E |
| 阶段 2 | 待定 | 2026-08-10 | 待定 | 进行中：数据库、普通管理、导入、任务、版本、自动审核及两个默认关闭 Worker 的后端实现已完成 | 真实 UNC/ACL、集成验证与完整平台 baseline |
| 阶段 3 | 待定 | 2026-08-10 | 待定 | 进行中：项目、镜头和资产后端/真实页面已接入，两类 Excel 与目录操作后端闭环已完成；项目、镜头和资产三个子集旅程通过 | 资产模板、人工需求处理、真实 NAS 部署门禁 |
| 阶段 4 | 待定 | 2026-08-11 | 待定 | 后端与真实任务工作台/详情/开始/编辑前端已实现；任务隔离浏览器子集通过 | 权限撤权负向、资产需求人工处理、完整 E2E |
| 阶段 5 | 待定 | 2026-08-12 | 待定 | 主文件版本、视频短期播放票据、Range 代码链和缩略图/网页代理派生 Worker 已实现 | 真实 UNC、FFmpeg 视频派生、MP4/MOV 206/416 浏览器旅程、codec/媒体轨 |
| 阶段 6 | 待定 | 2026-08-11 | 待定 | 自动单版本审核和 `manual_batch` 状态机已接入；票据视频播放、时间点、四类批注、A/B 对比和退回再提交入口通过定向检查 | 审核浏览器旅程 |
| 阶段 7 | 待定 | 2026-08-12 | 待定 | 工作台、待我审核、最近提交、单任务版本工作区、审核单、文件/NAS 一级页与统一全局搜索已接真实 API；搜索定向前端单测与后端 Ruff 通过 | 审核/文件/搜索浏览器旅程及跨项目越权集成测试 |
| 阶段 8 | 待定 | 待定 | 待定 | 项目、镜头管理/导入、资产管理/导入及任务工作台/版本上传四个子集已完成真实后端/PG/Redis/账号/生产代理隔离旅程 | 真实 UNC、审核/文件、权限负向及阶段 1—7 其余链路，完整系统 E2E |

## 19. 建议立即开始的工作

镜头、资产样表参数，项目/成员/集/场次/镜头/资产/制作分项普通管理、两类 Excel 预检与提交、独立任务、主文件版本和 `auto_single` 审核后端，以及两个默认关闭的 NAS Worker 已完成代码实现；独立业务前端项目、镜头、资产、任务工作台和单任务版本工作区均已接入，四个隔离子集浏览器门禁已关闭。当前先补齐缺失功能，再集中进入严格验收，按以下顺序继续：

1. 补齐媒体缩略图/代理，每个迭代批次只做最小定向检查。
2. 缺失 MVP 功能基本收敛后，集中补充项目总监/制作人员权限撤权、全局搜索跨项目越权、异常权限矩阵、真实 MP4/MOV Range 206/416、failed 原行 retry、未知 create 重放和 PostgreSQL 集成验证。
3. 最终验收阶段再使用正式 Windows Worker 服务账号和隔离 UNC 根目录完成 NAS/AD/共享 ACL、目录与版本 Worker、失败重试、租约接管、Leader 接管和业务下载授权；验收前两个 Worker 保持默认关闭。完成上述门禁后，再执行完整浏览器交付闭环：

```text
真实登录
→ 项目列表
→ 创建项目
→ 添加项目成员
→ 导入一个最小镜头表并生成集、场次、镜头和待匹配资产需求
→ 导入一个最小资产表并自动匹配唯一需求
→ 刷新后数据与权限保持正确
```

任务工作台、待我审核/最近提交、人工批量审核单、统一全局搜索、四类批注、视频短期票据 Range 代码链，以及缩略图/网页代理派生与展示均已接入。下一批优先补齐媒体派生人工重试/运维状态；真实 FFmpeg 视频派生、MP4/MOV 206/416、UNC 及审核/文件/搜索浏览器旅程留在对应集中门禁验证。
